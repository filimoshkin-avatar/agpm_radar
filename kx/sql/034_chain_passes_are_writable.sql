BEGIN;

SET search_path = kx, public;

-- ---------------------------------------------------------------------------
-- Таблица, в которую никто не может писать
--
-- 033 завела `chain_passes`, дала читателю `agent.sync` и забыла единственное,
-- ради чего таблица существует: право рабочей роли писать в неё. Все двенадцать
-- миграций до неё, заводившие таблицы, кончались строкой `GRANT ALL ON <table>
-- TO radar_kx`; эта - нет.
--
-- Гейт миграций при этом был зелёный. Он применяет SQL под административной
-- ролью и проверяет, что схема встала; чего он не проверял - может ли рабочая
-- роль сделать с новой таблицей то, ради чего она заведена. На проде это
-- выглядело так: опрос периметра отработал, напечатал свой JSON и упал на
-- закрытии прохода - «permission denied for table chain_passes». Таймер
-- продолжал бы падать каждые полчаса.
--
-- Тот же урок, что и раньше: зелёный гейт значит «исполненное верно», а не
-- «исполнилось всё». Вместе с этой миграцией заведён тест, который требует
-- INSERT для `radar_kx` на каждой таблице `kx`, - чтобы следующая забытая
-- строка красила гейт, а не прод.
-- ---------------------------------------------------------------------------

GRANT ALL ON chain_passes TO radar_kx;

UPDATE metadata SET value = '34'::jsonb, updated_at = clock_timestamp()
WHERE key = 'schema_version';

COMMIT;
