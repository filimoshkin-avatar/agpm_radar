# Radar V2 Knowledge Base: инженерный план развития

Дата: 2026-08-22

Редакция 5.2. Все продуктовые развилки закрыты решениями владельца P1-P34. Учтено финальное
независимое ревью: закрыты пережитки отменённых формулировок, восстановлены два deliverable,
утерянных при переписывании редакции 5 (путь импорта локальных и операторских файлов; срез
браузерного рендеринга), определены границы вертикального среза и состав срезов 1.3 и 1.6.

**Статус: решений владельца больше не требуется; контракты подлежат написанию.** Блокирующих
вопросов нет. План становится исполнимым после того, как в фазе 1 будут написаны и утверждены
corpus-membership contract, ADR-0004…ADR-0008, схема-спека 003 и контракты §9.3 — это работа, а не
развилка. Срезы фазы 1 можно начинать немедленно.

Это единственный канонический план. Параллельных roadmap нет:
`docs/radar-v2-knowledge-base-roadmap-2026-08-22.md` — историческая запись направления владельца,
как план отменён.

GRACE-Delta: skip — в Radar нет карты модулей `M-*`/`V-M-*`.

---

## 0. Delta редакции 4

### 0.1. Что изменено

| Область | Было (ред. 3) | Стало (ред. 4) |
|---|---|---|
| Egress полных текстов | «Полный текст не покидает Local Ru никогда» | Постоянное **хранение** остаётся на Local Ru; контролируемый **egress для обработки** к двум allowlisted-провайдерам разрешён (P18) |
| Сетевая политика извлечения | «Профиль извлечения без доступа в интернет» | Два юнита, две политики: оркестратор без интернета, профиль Hermes с egress только к утверждённым эндпоинтам |
| Публикация доказательств | Всё ждёт одобрения владельца | Структурный слой (цитаты, цифры, переводы) публикуется автоматически при fail-closed условиях; авторские тексты остаются под одобрением (P19) |
| Q&A | Один публичный агент | Два режима с разными scopes: public по опубликованному срезу, authenticated research/editor по полному KX (P20) |
| Редакторский режим | Внутренний, вне публичного контура | На публичном домене после входа, серверная авторизация на каждый privileged endpoint (P21) |
| Рейтинги | Четыре оценки на идею | Двухуровневая оценка материала: концептуальная — относительно конкретного knowledge release wiki; источниковая — относительно полного KX. Версионируемый re-score (P22) |
| Граф | Понятия, сущности, материалы вперемешку | v1: приоритет 1 — concepts/ideas/claims вокруг wiki; приоритет 2 — доказательная трасса. Компании, продукты, роли и временная эволюция — расширения, не определяющие UX v1 (P23) |
| Верификатор | Проверка чисел, дат, кавычек, ссылок | Плюс claim binding: каждое фактическое утверждение ссылается на accepted `claim_id` и exact evidence; свободный текст без привязки запрещён |
| Кандидаты извлечения | Предлагалось добавить `candidate` в `claim_evidence` | Отдельная сущность `extraction_candidates`; `claim_evidence` остаётся exact-only |
| Независимость источников | Два документа = два подтверждения | Кластеры source-family и content-duplicate; два документа одной семьи не усиливают доказательство |
| Классы доступа | «Юридическая сторона закрыта, остался риск блокировки» | Формулировка убрана. Разделены классы: robots, публично доступная страница, paywall, auth, технический контроль доступа |
| Масштабирование | Сразу 275 документов | Сначала вертикальный срез 20-30 документов от ingest до knowledge release с порогами приёмки |
| Единицы счёта | 284 / 280 / 277 / 275 без объяснения | Отдельный corpus-membership contract (§6) как предусловие таксономии, gold-наборов и claims |
| **Wiki** | «Построить wiki: вывести 40-80 понятий из 275 статей» | **Wiki уже существует** и ведётся Project Manager по написанной схеме (§6А). Задача — не породить таксономию, а связать существующие compiled-страницы с доказательствами KX и опубликовать |
| **Таксономия тем** | 8 тем, выведенных снизу из корпуса | **Онтология AgPM**, уже скомпилированная в `agpm/wiki/` (принципы, области практик, ответственность, зрелость, риски, данные, организационный контекст, мультиагентность) |
| **Типы связей графа** | Придуманный словарь supports/contradicts/refines | **Существующий словарь SCHEMA.md**: `supports`, `extends`, `constrains`, `contradicts`, `operationalizes`, `depends-on` |
| **Схема страницы понятия** | Придуманная | **Существующие page conventions**: Purpose, Core claims, Supporting sources, Tensions / Contradictions, Implications for AgPM model, Open questions |

### 0.1а. Что добавила редакция 5

| Область | Решение |
|---|---|
| Канон и стандарты | Загружаются в KX отдельным корпусом. Новый срез 1.6 (P25) |
| Ведение wiki | Остаётся у Project Manager; редакторский режим только одобряет публикацию (P26) |
| Версионирование wiki | Снимок при каждой публикации, git не вводится. Принятая цена — нет истории между публикациями (P27) |
| Два хранилища текстов | Разные роли плюс обязательный отчёт сверки, новый срез 2.4а (P28) |
| Пороги качества | Не назначаются заранее: измеряются на срезе, планка ставится до масштабирования (P29) |
| Передача данных провайдеру | Не блокируется отсутствием гарантий retention; аудит сохраняется (P30) |
| Доступ к редактору | Владелец плюс приглашённые: учётные записи, две роли, права на каждом endpoint (P31) |
| Длина цитаты | До абзаца с атрибуцией и ссылкой; дифференциация по типу источника отменена P34 (P32) |
| Вопросы читателей | Хранятся бессрочно; отсюда политика конфиденциальности как условие запуска, ограничение доступа и удаление по запросу (P33) |

### 0.1б. Что добавила редакция 5.2

| Область | Решение |
|---|---|
| Стандарты и авторитетные материалы | **P34: ограничений нет.** Radar — разработчик концепции и Манифеста агентного управления проектами, представляет экспертное сообщество, вопрос согласован с авторами. Отменены дифференциация длины цитаты по типу источника, поле `premium_boundary`, разделение «загрузка против цитирования» и разрешительные оговорки в §11.5 и срезе 1.6 |
| Длина публичной цитаты | Единая: до абзаца с атрибуцией и ссылкой, для всех типов источников |

### 0.2. Какие противоречия закрыты

1. **Схема против контракта.** §8 требовал `claim_evidence` только для exact, модель данных предлагала
   класть туда же `candidate`. Разведено: `claim_evidence` exact-only с триггером, кандидаты — в
   отдельной таблице с переходом в exact только через явную валидацию.
2. **Egress против размещения.** Обоснование «разбор на Local Ru, потому что текст не должен уезжать»
   рассыпалось после P18. Размещение сохранено, но по другим основаниям: локальность к БД, единая
   точка хранения, наличие утверждённого шлюза моделей. Основание записано честно.
3. **Одобрение против ежедневности.** Требование одобрять всё вручную конфликтовало с обещанием
   ежедневного автоматического обновления. Разведено по слоям: структурный — автоматически,
   авторский — с одобрением.
4. **Редактор против границы.** Редакторский режим был описан и как внутренний, и как часть публичного
   интерфейса. Зафиксировано: публичный домен, серверная авторизация, KX остаётся loopback-only.
5. **«Юридически закрыто».** Продуктовое решение владельца было записано как юридическое заключение.
   Формулировка убрана, классы доступа разделены.

### 0.3. Что ещё требует решения владельца

**Ничего блокирующего.** Отложены по существу и с определённым моментом принятия (§15.2): планка
качества после вертикального среза; источник эмбеддингов, если лексики не хватит; судьба ленты
выпусков после отчёта расхождений.

---

## 1. Owner decision log

| # | Решение | Следствие |
|---|---|---|
| P1 | Аудитория — все читатели Radar, публично | Публикуемый срез знаний и публичная поверхность; KX закрыт |
| P2 | Наружу **публикуются** только цитаты и наша авторская переработка. Полные тексты чужих материалов не перепубликуются никогда | Ограничение на **публикацию**, не на обработку. См. P18 |
| P3 | Фокус — агентное управление. Смысловое ядро — канон AgPM (`manifesto.aipractice.space`: Манифест, White Paper v1.0) | Извлечение и таксономия ведутся под этим углом; список понятий засеивается из канона, а не только снизу из статей |
| P4 | Публикация гибридная | Уточнено P19: структурный слой автоматически, авторский — с одобрением |
| P5 | Рубрикатор: оси тема / жанр / понятия | Двухуровневая навигация |
| P6 | Четыре измерения оценки | Уточнено P22: двухуровневая схема, без сводного балла |
| P7 | Русский; цитата сразу в переводе, оригинал доступен | Перевод — версионируемая сущность при span'е оригинала |
| P8 | Бюджет не ограничен, приоритет — качество | Этапность определяется измеримостью, не стоимостью |
| P9 | На Local Ru только две модели: `zai/glm-5.2`, `minimax/MiniMax-M3` | Список не расширяется. См. P18 про egress к ним |
| P10 | Пользователи работают через интерфейс Radar; вызовы моделей — через Hermes | Hermes не публикуется и не является автором ответа |
| P11 | Полнотекстовый сбор — приоритет; способ добычи не имеет редакционного значения | Провенанс — техническая запись способа. См. исправление классов доступа в §11.5 |
| P12 | Первый публичный запуск — всё сразу: wiki, навигация, граф, рейтинги, агент. Референс наполнения `https://pimenov.ai/knowledge/`; интерфейс на решениях Radar через Claude Design; переключатель `радар / газета / агент` | Точка встройки: `v2/apps/web/index.html`, `nav.view-switcher`, логика в `app.mjs` |
| P13 | Идеи порождает машина, владелец одобряет формулировки | Отдельный срез порождения идей-кандидатов |
| P14 | Одобрение — режим редактора внутри интерфейса базы знаний | Уточнено P21: тот же публичный домен, вход по авторизации |
| P15 | Ритм одобрения — 15-30 минут ежедневно | Нагрузка снижена P19: структурный слой одобрения не требует |
| P16 | Лента выпусков пока не трогается; сначала отчёт расхождений на продуктиве | Отчёт — отдельный deliverable |
| P17 | Состав wiki: машинный список понятий с числом подтверждений, владелец вычёркивает | Целевое число страниц заранее не назначается |
| **P18** | **Egress полных текстов разрешён.** Полные документы и необходимые фрагменты можно передавать с Local Ru через Hermes во внешние API **только двух утверждённых моделей** (GLM, MiniMax). Постоянное хранение остаётся на Local Ru | Обязательны: минимизация контекста, аудит каждого вызова, deny-by-default для прочего egress, запрет retention/training там, где это поддерживается API или условиями провайдера |
| **P19** | **Цитаты, цифры и переводы публикуются автоматически**, без ручного и пакетного approval-gate, при выполнении fail-closed условий (§8.4). Авторские тексты wiki и формулировки инсайтов остаются под одобрением владельца | Разделение публикационных путей структурного и авторского слоёв |
| **P20** | **Два режима Q&A.** Public отвечает только по immutable published knowledge release. Authenticated research/editor ищет по всему допустимому complete full-text KX | Наличие документа в KX не означает его публичность. Разные scopes, авторизация, лимиты, аудит и наборы приёмки |
| **P21** | **Редакторский режим на публичном домене после входа** (например `radar.agpm.space/editor`). Тот же продуктовый интерфейс, но серверная авторизация обязательна на каждом privileged endpoint | KX остаётся loopback-only. Исключить обход scopes, IDOR, утечку черновиков и полного текста через public API |
| **P22** | **Рейтинги строятся вокруг wiki.** При разборе каждого материала выпуска Project Manager даёт двухуровневую оценку: концептуальная применимость и новизна — относительно **конкретного immutable knowledge release** wiki; новизна источника, независимость доказательств, duplicate/source-family статус и 90-дневный моментум — относительно **полного KX** | Хранить `knowledge_release_id`, версии рубрики, промпта и модели, объяснение и evidence spans. Исторические оценки не переписываются молча; re-score — отдельное версионированное событие. Измерения не сводятся в один непрозрачный балл |
| **P33** | **Вопросы читателей хранятся бессрочно** | Требуются политика конфиденциальности как условие запуска, ограничение доступа к архиву вопросов и механизм удаления по запросу |
| **P34** | **Ссылки на стандарты и подобные авторитетные материалы разрешены.** Radar представляет экспертное сообщество, является разработчиком концепции и Манифеста агентного управления проектами, и вопрос согласован с авторами. Отдельных ограничений на хранение и цитирование таких материалов нет | Отменяет дифференциацию P32 по типу источника, поле `premium_boundary`, разделение «загрузка против цитирования» и все связанные оговорки в срезе 1.6 и §11.5 |
| **P32** | **Длина публичной цитаты**: до абзаца, с атрибуцией и ссылкой на источник. Дифференциация по типу источника отменена P34 | Одно правило на всё, проверяется автопубликацией (P19) |
| **P31** | **Редакторский режим — владелец плюс несколько приглашённых** | Нужны учётные записи, роли (редактор с правом публикации против исследователя без него) и проверка прав на каждом privileged endpoint |
| **P30** | **Тексты передаются во внешние модели независимо от наличия гарантий retention/training** | Допущение о гарантиях снято, работа не блокируется. Аудит каждого вызова сохраняется — он нужен нам самим |
| **P29** | **Пороги retrieval и доли отказов заранее не назначаются**: измеряются на вертикальном срезе, планка ставится владельцем **до** масштабирования на 275+ | Нулевые пороги на числа, переводы, независимость источников, инъекции и авторизацию остаются в силе и не обсуждаются |
| **P28** | **Файловое хранилище полных текстов и KX сохраняют разные роли**: файлы — рабочая копия Project Manager, KX — доказательная база публикации | Обязателен регулярный отчёт сверки: без него расхождение обнаружится не тогда, когда возникнет, а когда сломает публикацию |
| **P27** | **Состояние wiki фиксируется снимком при каждой публикации**; git не вводится | `knowledge_release_id` реализуется снимком с контрольной суммой. Принятая цена: между публикациями истории изменений нет, «кто и зачем менял» не восстанавливается |
| **P26** | **Wiki ведёт Project Manager, как сейчас.** Редакторский режим одобряет публикацию, но не является основным местом правки текста | Один пишущий контур, конфликтов слияния нет. Правка страницы идёт через агента |
| **P25** | **Канон AgPM и внешние стандарты загружаются в KX** как отдельный корпус (White Paper, Манифест, обоснование онтологии, AgPMBoK, ГОСТ Р 72514-2026, ISO 21502, PMBOK 8, PMI AI PPPM, NGMN, Sber, arXiv) | Утверждения wiki получают точные ссылки на источник. Ограничений на хранение и цитирование нет (P34) |
| **P24** | **Wiki — это существующая база знаний AgPM**, которую ведёт Project Manager: `/root/.openclaw-projectmanager/workspace/knowledge/agpm/` (концептуальная, 93 файла) и `.../knowledge/agpm-radar/` (радарная, 204 файла). Точка входа `agpm/index.md`, скомпилированная модель `agpm/wiki/overview/agpm-overview.md`, правила ведения `agpm/SCHEMA.md` | Продукт базы знаний — **публикуемая проекция существующей wiki, подкреплённая доказательствами KX**, а не новая wiki. Её схема, словарь связей и конвенции страниц принимаются как есть |
| **P23** | **Graph v1**: приоритет 1 — concepts / ideas / claims и смысловые связи вокруг wiki; приоритет 2 — доказательная трасса `claim → exact evidence → document → source family` с `supports` / `contradicts` / `refines`. Компании, продукты, роли и временная эволюция — расширяемые будущие представления, не определяющие UX v1 | Ограничивает объём графа в первом запуске |

---

## 2. Superseded формулировки

Утверждения предыдущих редакций, которые более не действуют. Перечислены явно, чтобы они не
всплыли из старых цитат.

| Отменено | Чем заменено |
|---|---|
| «Чужой полный текст не покидает Local Ru никогда» | P18: не покидает **место постоянного хранения**; контролируемый egress для обработки разрешён |
| «Профиль извлечения закрывается так, что у него ноль доступа в интернет» | §11.7: без интернета остаётся **оркестратор**; профиль Hermes имеет egress по allowlist на два эндпоинта |
| «Разбор на Local Ru, потому что иначе появится вторая копия чужого текста» | §11.7: размещение сохранено по другим основаниям — локальность к БД, единая точка хранения, утверждённый шлюз моделей |
| «Неодобренное физически отсутствует в срезе» — в применении ко всему содержимому | P19: относится к авторскому слою. Структурный слой публикуется автоматически при выполнении fail-closed условий |
| «Редакторский интерфейс — внутренняя поверхность вне публичного контура» | P21: публичный домен, серверная авторизация на каждый privileged endpoint |
| «Публичный агент — единственный режим Q&A» | P20: два режима с разными scopes |
| «Решением владельца юридическая сторона вопроса закрыта; остаётся чисто техническая проблема» | §11.5: это продуктовое решение владельца, а не юридическое заключение. Классы доступа разделены |
| «Единственный оставшийся операционный риск — блокировка» | §11.5: блокировка — один из рисков; отдельно стоят классы доступа, пересекать которые лестница не должна |
| «`claim_evidence`: добавить состояние `candidate`» | §10.2: `claim_evidence` остаётся exact-only; кандидаты — в `extraction_candidates` |
| «Верификатор проверяет числа, даты, цитаты и ссылки» | §8.3: плюс обязательная привязка каждого фактического утверждения к `claim_id` |
| «Два документа подтверждают утверждение независимо» | §10.4: только если они из разных source-family и не являются content-duplicate |
| «План готов к исполнению» | Статус в шапке: контракты в проработке |
| «Машина выводит список из 40-80 понятий из 275 материалов, владелец вычёркивает» | P24: понятия уже существуют в `agpm/wiki/`. Машина предлагает **дополнения** к существующей структуре, с числом подтверждений; владелец решает по дополнениям (P17 сохраняется в этой части) |
| «8 тематических разделов как хребет навигации» | Хребет — онтология AgPM из `agpm-overview.md`; изобретать параллельную рубрикацию не требуется |
| «Словарь типов рёбер определяется в схеме KX» | Словарь задан в `agpm/SCHEMA.md` и переносится в схему без изменений |

---

## 3. Текущее состояние

### 3.1. Контуры

- Legacy production `https://radar.aipractice.space` — независим, не трогаем.
- Production Radar V2 на Local Ru `https://radar.agpm.space` — активный контур разработки.
- Ежедневный dual-run: Legacy 08:00 МСК, V2 08:25 МСК (задача `0af53bc1-2fea-4b66-bce6-b5d3a8e4f064`,
  `--command-cwd /mnt/vdd/Radar/v2`, failure-alert в Telegram).
- Radar KX — loopback-only PostgreSQL 17 `radar_kx` на Local Ru; расширения `pgcrypto`, `pg_trgm`,
  `unaccent`, `vector` 0.8.6; таймеры `radar-kx-ingest.timer`, `radar-kx-backup.timer`.
- Hermes Agent 0.20.0 на Local Ru, шлюз на `127.0.0.1:8642`, модели `zai/glm-5.2` и
  `minimax/MiniMax-M3`; рядом два API-only профиля NRD как образец изоляции.
- Канон AgPM: `manifesto.aipractice.space` — Манифест и White Paper v1.0; сайт
  `agpmo.aipractice.space`.
- **Существующая wiki** (P24, §6А): `/root/.openclaw-projectmanager/workspace/knowledge/agpm/` и
  `.../knowledge/agpm-radar/` — 93 и 204 файла, ведёт Project Manager. Не под git.

### 3.2. Замер production 2026-08-22 (read-only)

Корпус KX: 8 310 материалов, 15 963 ревизии, 8 313 документов, 6 792 версии (6 489 complete),
6 679 raw blobs, **19 010 чанков**, **59 242 604 символа** канонического текста complete-версий
(в среднем 9 130 на версию), БД **688 MB**, очередь 5 933 succeeded / 2 378 failed.

Версии по источнику: `network` 6 120 (5 895 complete), `legacy_snapshot` 578 (569),
`legacy_truncated` 69 (0 complete), `operator_artifact` 25 (25).
Язык complete-версий: ru 4 305, en 1 839, mixed 317, und 28 — сумма 6 489, сходится.

Периметр текущего релиза `rel_473de6c600563860305e2bd0`: 77 выпусков, 49 с отобранными материалами,
**277 строк отбора, 275 документов, 1 098 чанков, 3 847 229 символов**, в среднем 13 990 символов на
документ (минимум 215, максимум 193 217), полный текст 275/275.

Причины отказов — срез 05:55, до закрытия 25 документов: `robots_denied` 1 878 (Reddit 1 745),
`http_403` 309, `weak_or_missing_text` 88, `timeout` 41, `http_429` 40, `http_404` 28,
`network_error` 11. На парсер приходится 88 строк из ~2 400.

Local Ru: 4 vCPU, 7 936 MB RAM, ~5 813 MB доступно, swap 8 GB присутствует, 50 GB свободно.
Control-хост: 4 vCPU, ~5 GB RAM, 6,5 GB свободно.

---

## 4. Ревизия рубрикатора

Разбор `data/db/radar.sqlite`: 77 выпусков, 284 материала, 11 рубрик, 818 связей.

**R1. Разметку ни разу не делал LLM.** Все 818 связей и все 284 записи классификации имеют
`source='fallback'`; пояснение записано самим классификатором: «LLM-провайдер не настроен». Вход —
только заголовок, аннотация, URL, имя источника и редакционный вывод; полный текст не использовался.

**R2. Жёсткий срез топ-3.** `pipeline/scripts/agpm_radar_llm_classify.py:314`. У 259 из 284 (91,2%)
ровно три рубрики, ровно одна — у 9.

**R3. Уверенность — константа.** Строка 315: 0,58 или 0,66. В базе 809 × 0,66 и 9 × 0,58.

**R4. Свалка по умолчанию — механизм есть, масштаб мал.** При нулевых очках ставится
`enterprise_adoption`, но единственной рубрикой она стоит лишь у 2 материалов из 284.

**R5. Перекос.** Процессы и оркестрация 157 (55,3%), Исследования и методология 131, AgPM/PMO 113,
Governance 101, Ответственность человека 87, Внедрение 75, Вендоры 59, Безопасность 58,
Инфраструктура и MCP 18, ИСУП 10, Инвестиции 9.

**R6. Смешаны три оси.** Тема, жанр материала (исследования, вендорские релизы) и тип события
(инвестиции и сделки) конкурируют за одно из трёх мест. Самая частая пара —
`research_methodology` + `workflow_orchestration`, 84 раза: это тема и жанр, а не две темы.

**Целевая структура (P5, уточнена P24).** Оси:

- **тема** — не изобретается: это онтология AgPM, уже скомпилированная в `agpm/wiki/`
  (принципы, инфраструктура данных, четыре области практик, двухслойная ответственность, модель
  зрелости, организационный контекст, мультиагентный режим, сравнения со стандартами);
- **периметр** — дальний / средний / близкий, уже существует и в радарной wiki, и в схеме V2;
- **жанр** — исследование, релиз вендора, сделка, мнение, кейс, регулирование, новость. Частично
  выводится из существующей `wiki/overview/corpus-genre-map.md`, которую надо сверить, а не
  переписывать;
- **понятия** — существующие страницы `wiki/entities/` и разделов модели; машина предлагает
  **дополнения** с числом подтверждающих материалов, владелец решает по ним (P17).

Переразметка обязательна независимо от структуры: текущие ярлыки не являются суждением.

---

## 5. Инвентаризация KX

### 5.1. Реализовано и работает

Источник: `corpus_imports`, `source_materials`, `source_material_revisions` (append-only),
`material_documents`, `documents`. Сырьё: `raw_blobs`, `fetch_attempts` (immutable), `fetch_queue`.
Канонический текст: `document_versions` (immutable, untruncated, `parser_config_sha256` в строке),
`reparse_runs`. Чанки: `chunks` ≤4 000 символов с точными offsets и готовыми `search_ru`/`search_en`
GIN-индексами. Периметр: `issue_perimeter_sources`, `issue_perimeter_members`, view
`issue_perimeter_documents`. Заготовки: `processing_runs`, `embedding_models`, `chunk_embeddings`,
`entities`, `entity_aliases`, `entity_merges`, `claims`, `claim_evidence` + триггер точного span'а,
`metrics`, `relations`, `ideas`, `idea_evidence`, `idea_scores`.

Триггер `validate_exact_claim_evidence` принимает доказательство только при точном совпадении
подстроки и SHA-256 цитаты. `canonicalize_text` делает NFC, поэтому символьные смещения PostgreSQL
и Python совпадают — инвариант, закрываемый тестом.

### 5.2. Дефекты

**D1 (критично).** Дрейф схемы: `operator_artifact` есть в production-констрейнтах, в
`kx/sql/002_issue_perimeter.sql` — нет. Схему production нельзя воспроизвести из репозитория.

**D2.** `SCHEMA_VERSION = 2` (`database.py:33`) — жёсткий гейт. Миграция требует порядка «БД, потом
релиз» и идемпотентности к применённым вручную ALTER.

**D3 (латентный).** View `issue_perimeter_documents` не фильтрует по `perimeter_source_id`. Сегодня
union и текущий релиз совпадают (275/275, из старого снимка выбыло 0, добавилось 8). Расхождение
возникнет только после коррекции, снимающей материал с опубликованного выпуска.

**D4.** `chunk_embeddings.embedding vector` без размерности (`001_initial.sql:217`) — индекс не
построить, смешение моделей проходит молча.

**D5.** У `claim_evidence` нет состояния «кандидат» — и не должно быть (§10.2): кандидаты живут в
отдельной сущности.

**D6.** Derived-слой без происхождения и review-состояний.

**D7.** Ограничения ingest-юнита: `MemoryDenyWriteExecute=true` несовместим с JIT V8;
`IPAddressDeny=127.0.0.0/8` при разрешённом `127.0.0.53/32`; `MemoryMax=3G`; `ProtectSystem=strict`
без `ReadWritePaths`.

**D8 (снято).** Ограничение «на Local Ru нет доступа к моделям» устарело: Hermes установлен
2026-08-18. Действует P9 и P18.

**D9 (подтверждено).** Две версии выпуска 77 (Appian, Futurum) имеют `source_kind='operator_artifact'`
при `quality='trafilatura'`; в `fetch_attempts` — успешная попытка `http_status=200` с пояснением
«direct browser-header HTTP fetch» в поле `error_detail`; по тем же URL есть более ранние
`robots_denied`; в очереди `robots_override=false`. Аудируемый механизм существует (33 попытки
`network_robots_override`, 23 флага) и не был использован; версий этого типа в корпусе ноль.

**D10.** `parser.py:67` — определитель языка считает кириллицу против латиницы. Испанский станет
`en`, китайский `und`.

**D11.** Публикуемого слоя в схеме нет.

**D12.** Тип источника не входит в идентичность версии: `version_id` =
`sha256(document_id, raw_sha256, parser_config_sha256, text_sha256)` (`identifiers.py:66-74`) и
`UNIQUE (document_id, raw_sha256, parser_config_sha256, canonical_text_sha256)`
(`001_initial.sql:133`). Исправить провенанс «новой версией» невозможно — коллизия по PK и UNIQUE.
Решение: append-only таблица провенанс-коррекций, ссылающаяся на существующую версию. Строки
`fetch_attempts` immutable и остаются как есть.

**D13 (новый).** Нет модели независимости источников: две записи в `documents` не означают два
независимых подтверждения. Синдикация, перепечатки и общий первоисточник в схеме не отражены. См.
§10.4.

---

## 6. Corpus-membership contract

**Предусловие.** Этот контракт должен быть согласован **до** таксономии, gold-наборов, извлечения
claims и любого массового переразбора. Без него неясно, что именно размечается и по чему считаются
метрики.

### 6.1. Наблюдаемое расхождение единиц счёта

| Слой | Единица | Значение | Источник истины |
|---|---|---|---|
| Редакционная база Legacy | `materials` | **284** строки, **280** уникальных `canonical_url` | `data/db/radar.sqlite` |
| Отбор в выпуски V2 | `issue_materials` | **277** строк отбора | активный content release V2 |
| Документы периметра | нормализованный `canonical_url` | **275** | `kx.issue_perimeter_members` → `kx.documents` |
| Корпус KX | `documents` | **8 313** | `kx.documents` |
| Полнотекстовые версии | `document_versions` где `is_complete` | **6 489** на 5 979 документов | `kx.document_versions` |

Проверено: `rejected_materials_internal` не пересекается с `materials` по URL, поэтому «7 лишних —
это отклонённые» неверно. Расхождения 284 → 280 → 277 → 275 не объяснены.

### 6.2. Что контракт обязан зафиксировать

1. **Единица счёта на каждом слое** и правило перехода между слоями.
2. **Правила канонизации URL и дедупликации** — одинаковые ли они у Legacy-пайплайна и у KX. Сейчас
   KX считает `document_id = sha256(canonical_url)`, а Legacy хранит и `url`, и `canonical_url`;
   совпадают ли нормализации, не проверено.
3. **Current vs historical perimeter**: что считается текущим периметром (последний снимок),
   историческим (union всех снимков) и как отчёты обязаны указывать, какой из них показывают (D3).
4. **Источник истины на каждый вопрос**: «сколько материалов в выпуске» отвечает V2;
   «есть ли полный текст» отвечает KX; «что размечено» отвечает слой таксономии.
5. **Что размечается и что оценивается** — 284 материала Legacy, 277 строк отбора или 275
   документов. От этого зависит и переразметка, и знаменатель всех метрик покрытия.

### 6.3. Гейт

Контракт считается согласованным, когда каждое из чисел 284 / 280 / 277 / 275 объяснено
воспроизводимым запросом, а не описанием. Разбор расхождения — первая работа фазы 1 и предусловие
всего остального.

---

## 6А. Существующая wiki и её отношение к KX (P24)

Это самая крупная поправка редакции 4. Wiki не является предметом разработки — она существует,
работает и ведётся по написанным правилам.

### 6А.1. Что уже есть

**`knowledge/agpm/` — концептуальная база AgPM: 93 markdown-страницы (113 файлов всего, включая DOCX/PDF-оригиналы), 40 MB.**

| Слой | Содержание |
|---|---|
| `SCHEMA.md` | Правила ведения: слои, обработка источников, конвенции страниц, словарь связей |
| `raw/` | Immutable извлечённые тексты. **Полные:** White Paper v1.0 и v1.2, Манифест v1/v2/v3, обоснование онтологии, AgPMBoK v0.7, ГОСТ Р 72514-2026, NGMN, Sber AI-Disrupt, AWS, две статьи arXiv. **Только выдержки:** ISO 21502, PMBOK 8. **Отсутствует в `raw/`:** PMI AI PPPM — есть лишь source-note в `wiki/sources/`. Плюс `originals/` (19 DOCX/PDF/ZIP) |
| `wiki/overview/` | Скомпилированная модель `agpm-overview.md`, онтологическая структура, карта корпуса, карта жанров, реестр документов, дельты ревизий |
| `wiki/principles/`, `practices/`, `data/`, `responsibility/`, `risks/`, `maturity/` | Разделы модели |
| `wiki/comparisons/` | Мосты к смежным подходам и стандартам |
| `wiki/entities/` | Узлы-понятия (например мультиагентная оркестрация, эффективность агента) |
| `wiki/sources/` | Заметки по отдельным источникам |
| `wiki/open-questions/`, `improvements/` | Нерешённое и планы доработки |
| `wiki/market/`, корневой `reviews/` | Рыночные снимки и ревью — в первоначальную опись не попали |
| `log.md` | Хронология ingest и изменений |

**`knowledge/agpm-radar/` — радарная база: 204 markdown-страницы (386 файлов всего, включая DOCX-отчёты и JSON), 231 MB.**

`sources.yml` (реестр источников и запросов; типы `telegram`, `habr_hub`, `reddit`, `arxiv_search`,
`ai_agents_directory_daily`, `rss`, `web`), `data/materials.jsonl` (инкрементальный реестр без
дублей), `data/source-fulltext/` (75 JSON, 1,2 MB), `runs/` (дневные журналы), `reports/`
(ежедневные MD и DOCX), `wiki/daily/` (compiled-снимки за каждый день, 2026-07-29 … 2026-08-22),
`wiki/monthly/` (накопительные сводки **для последующего переноса устойчивых выводов в основную
wiki AgPM**), `wiki/stats/`, `wiki/overview/` (вочлист дальнего периметра, инкрементальная модель
исследования, контроль ежедневной публикации), `incidents.md`.

### 6А.2. Что из этого принимается как данность

1. **Онтология AgPM — хребет таксономии.** Принципы, инфраструктура данных, четыре области практик,
   двухслойная ответственность, модель зрелости, организационный контекст, мультиагентный режим.
   Изобретать параллельные «8 тем» не требуется и вредно.
2. **Словарь связей** — из `SCHEMA.md`: `supports`, `extends`, `constrains`, `contradicts`,
   `operationalizes`, `depends-on`. Переносится в схему графа без изменений (§10.6).
3. **Конвенции страницы** — Purpose, Core claims, Supporting sources, Tensions / Contradictions,
   Implications for AgPM model, Open questions. Это готовая схема `concepts`/`concept_versions`
   (§10.1).
4. **Правила обработки источников** — raw неизменяем; каждый источник получает заметку в
   `wiki/sources/`; синтез-страницы обновляются при новом тезисе, уточнении или противоречии;
   противоречия размечаются явно; нормативные, дескриптивные и имплементационные утверждения
   разделяются. Это совпадает по духу с evidence contract KX и должно быть согласовано с ним, а не
   заменено.
5. **Три периметра** — дальний, средний, близкий — существуют и в радарной wiki, и в схеме V2
   (`perimeter IN ('near','mid','far')`).
6. **Контур переноса выводов** — `wiki/monthly/` → основная wiki AgPM — это уже работающий механизм
   самопополнения, только файловый. Проектируемый контур должен в него встроиться, а не подменить.

### 6А.3. Разделение ролей между двумя хранилищами

| | `knowledge/` (файлы, control-хост, ведёт Project Manager) | KX (PostgreSQL, Local Ru) |
|---|---|---|
| Источник истины для | скомпилированной модели AgPM, авторских синтез-страниц, редакторских решений | полного текста, immutable-версий, точных span'ов, провенанса |
| Единица | markdown-страница | документ, версия, чанк, span |
| Неизменяемость | `raw/` неизменяем по соглашению | обеспечена триггерами БД |
| Версионирование | **отсутствует: каталог не под git** | версии и хеши в схеме |
| Роль в продукте | что мы утверждаем | чем мы это доказываем |

**Продукт базы знаний = публикуемая проекция wiki, где каждое фактическое утверждение подкреплено
доказательством из KX.**

### 6А.4. Три проблемы — решения приняты

**1. Версионирование wiki — снимок при каждой публикации (P27).** Git не вводится. Перед сборкой
knowledge release состояние `knowledge/` целиком сохраняется в KX со своей контрольной суммой, и
именно этот идентификатор попадает в `knowledge_release_id` оценок (P22). От Project Manager
ничего не требуется.

Принятая цена, записанная явно: **между публикациями истории изменений нет.** Если страница
менялась трижды за неделю, в снимке останется только итог. Восстановить «кто и зачем менял» будет
нельзя. Это осознанный компромисс в пользу простоты; при необходимости git добавляется позже без
переделки снимков.

**2. Два хранилища с разными ролями (P28).** Файлы `agpm-radar/data/source-fulltext/` — рабочая
копия Project Manager; KX — доказательная база публикации. Расхождение неизбежно, поэтому
обязателен **регулярный отчёт сверки**: какие документы есть в одном и отсутствуют в другом, где
тексты различаются, какая версия новее. Отчёт не решает расхождение, но делает его видимым в момент
возникновения, а не в момент, когда оно сломает публикацию.

**3. Материалы радара учитываются в трёх местах.** `agpm-radar/data/materials.jsonl`,
`data/db/radar.sqlite` (284 материала) и `kx.documents` (8 313). Это прямо расширяет
corpus-membership contract (§6): единиц счёта не четыре, а больше, и правила канонизации в
файловом контуре надо сверить с KX.

### 6А.5. Как меняется задача

| Было | Стало |
|---|---|
| Вывести список понятий из 275 статей | Взять существующие compiled-страницы как хребет; машина предлагает **дополнения** с числом подтверждений, владелец решает (P17 в этой части сохраняется) |
| Придумать схему страницы | Использовать конвенции `SCHEMA.md` |
| Придумать словарь связей | Использовать словарь `SCHEMA.md` |
| Написать тексты wiki с нуля | Связать существующие утверждения страниц с claims и точными span'ами KX; дописывать недостающее |
| Построить контур самопополнения | Встроиться в существующий: `runs/` → `wiki/daily/` → `wiki/monthly/` → основная wiki |
| Изобрести рубрикацию | Онтология AgPM плюс три периметра, уже существующие |
| Утверждения wiki нечем подкрепить | Канон AgPM и внешние стандарты загружаются в KX отдельным корпусом (P25), и страницы получают точные ссылки |

## 7. Целевая архитектура

```
control-хост (редакционная плоскость, широкий выбор моделей)
  ├─ Project Manager OpenClaw — ежедневный выпуск Radar
  │     └─ двухуровневая оценка каждого материала (P22):
  │          уровень 1 — относительно конкретного knowledge release wiki
  │          уровень 2 — относительно полного KX (через authenticated research API)
  └─ Авторская переработка: тексты понятий, формулировки инсайтов
        рабочая копия текстов у PM (P28); полные тексты доступны
        и через research-scope (P20)
        │  SSH: структурированный экспорт → черновики обратно
        ▼
Local Ru — место постоянного хранения полных текстов
  ┌─ Оркестратор конвейера (новый юнит) ──────────────────────────┐
  │  без интернета: loopback к Hermes + unix-сокет PostgreSQL     │
  │  выбор документов, окна текста, выравнивание span'ов, запись  │
  └───────────────────────┬───────────────────────────────────────┘
                          │ loopback
  ┌─ Hermes: профиль извлечения (новый) ──────────────────────────┐
  │  egress ТОЛЬКО к утверждённым эндпоинтам GLM и MiniMax (P18)  │
  │  deny-by-default на всё остальное; аудит каждого вызова       │
  └───────────────────────┬───────────────────────────────────────┘
                          │ unix-сокет
  ┌─ Radar KX (loopback-only, наружу не открывается) ─────────────┐
  │  raw_blobs, fetch_attempts, document_versions, chunks,        │
  │  issue_perimeter_*, claims + claim_evidence (exact),          │
  │  extraction_candidates, source_families, concepts, wiki,      │
  │  translations, ideas, scores, graph                           │
  └───────────────────────┬───────────────────────────────────────┘
        авто (P19): цитаты, цифры, переводы при fail-closed
        одобрение (P4): авторские тексты и формулировки идей
                          ▼
  ┌─ Публикуемый срез знаний (immutable knowledge release) ───────┐
  │  атомарно переключаемый указатель, как content release V2     │
  └───────────────────────┬───────────────────────────────────────┘
                          ▼
  ┌─ Публичная поверхность ───────────────────────────────────────┐
  │  radar.agpm.space:  [Радар] [Газета] [Агент]                  │
  │    public scope — только опубликованный срез (P20)            │
  │  radar.agpm.space/editor — после входа (P21)                  │
  │    editor scope — research Q&A по всему допустимому KX,       │
  │    очередь черновиков, решения                                │
  │  Caddy — единственная публичная точка входа                   │
  └───────────────────────────────────────────────────────────────┘
```

Инварианты, которые не двигаются:

- **KX не имеет публичного порта, маршрута Caddy и записи DNS.** Доступ к полному тексту возможен
  только через authenticated editor API с серверной проверкой scope на каждом запросе.
- **KX не участвует в публикации выпусков V2 и не может её сломать.** Связь односторонняя.
- **Hermes не публикуется и не является автором ответа.** Отбор доказательств, сборка и верификация
  остаются в коде Radar.
- **Публичный `/api/search` V2 и поиск базы знаний — разные поверхности.**
- **Egress разрешён только к двум утверждённым эндпоинтам** и только из профиля Hermes.

---

## 8. Evidence contract

### 8.1. Общие правила

1. LLM не является источником точных значений: числа, даты, единицы, валюты, дословные цитаты и
   ссылки берутся из строк KX.
2. Единица доказательства — `(version_id, char_start, char_end, quote_sha256)`.
3. В `claim_evidence` попадает **только** `exact`; проверку делает триггер БД.
4. Невыровненные извлечения живут в `extraction_candidates` (§10.2) и в фактический слой, граф и
   ответы не попадают.
5. Числа, даты и единицы разбираются детерминированно из уже проверенного span'а.
6. Строгий режим fail-closed: нет основания — структурный отказ с указанием, чего не хватило.
7. Всё воспроизводимо: сохранённый прогон либо воспроизводится, либо отвергается.

### 8.2. Независимость доказательств

8. **Два документа не равны двум подтверждениям.** Усиление доказательства и допуск идеи требуют
   доказательств из разных `source_family` и не являющихся `content_duplicate` (§10.4).
9. Синдикация, перепечатка и общий первоисточник понижают вклад до одного подтверждения.

### 8.3. Claim binding (усиленный верификатор)

Проверок чисел, дат, кавычек и ссылок **недостаточно**: качественное неподтверждённое утверждение
может не содержать ни одного такого токена. Поэтому:

10. **В строгом режиме каждое фактическое предложение (атомарный factual clause) обязано ссылаться
    на accepted `claim_id` и на exact evidence этого claim.**
11. **Свободный текст модели без привязки к claim запрещён.** Связующие обороты, не несущие
    фактического содержания, допускаются и помечаются как несодержательные; всё остальное без
    привязки — ошибка.
12. При нехватке основания — **abstention**: структурный отказ, а не смягчённая формулировка.
13. Верификатор проверяет три уровня: привязка каждого clause к claim; совпадение токенов
    (числа, даты, единицы, имена, кавычки, ссылки) с процитированным span'ом; независимость
    источников там, где утверждение опирается на повторяемость.

### 8.4. Автоматическая публикация структурного слоя (P19)

Цитаты, цифры и переводы публикуются **без ручного и пакетного одобрения** при одновременном
выполнении условий. Любое невыполненное условие — элемент не публикуется и уходит в quarantine с
причиной:

1. оригинальная цитата **точно** совпадает с immutable span;
2. координаты, hash, URL и провенанс валидны;
3. цифры, даты, единицы и имена собственные проходят детерминированные проверки;
4. перевод показан **вместе с доступным оригиналом** и помечен как машинный;
5. элемент проходит проверку независимости источников там, где она применима.

Авторские тексты wiki и формулировки инсайтов под это правило **не** подпадают: они остаются под
одобрением владельца (P4).

### 8.5. Переводы

14. Перевод — хранимая версионируемая сущность при span'е оригинала, с автором (модель или человек),
    хешем промпта и состоянием проверки.
15. Оригинал всегда доступен; без сохранённого оригинала перевод не публикуется.
16. Числа, даты и единицы при переводе инвариантны посимвольно; расхождение — блокирующая ошибка.
16а. **Имена собственные сверяются не буквальным равенством, а через таблицу алиасов.** Требование
    «John Smith и Джон Смит — одно и то же» невыполнимо строковым сравнением между письменностями.
    Правило: имя в переводе принимается, если оно совпадает с оригиналом либо является
    зарегистрированным алиасом той же сущности в `entity_aliases` (таблица уже есть в схеме).
    Незарегистрированный вариант написания — не ошибка перевода, а **запрос на новый алиас**: он
    уходит в очередь, а цитата до решения не публикуется.
17. Перевод никогда не подменяет доказательство: проверка факта идёт по оригинальному span'у.

### 8.6. Публикация и ссылки

18. Публично цитируется выдержка до абзаца с атрибуцией и ссылкой на первоисточник (P32). Правило
    единое для всех типов источников, включая стандарты (P34). Полные тексты чужих материалов не
    публикуются (P2). Предел проверяется автопубликацией (§8.4) как обязательное условие.
19. Цитата и ссылка указывают на ту же версию, из которой взята цитата. Текст из веб-архива ссылается
    на архивный снимок с его датой.
20. Способ добычи на допуск к публичному цитированию не влияет (P11); ограничения задаются классами
    доступа (§11.5), а не типом источника.

---


## 9. API, scopes и авторизация

### 9.1. Два режима Q&A (P20)

| | **public** | **editor / research** |
|---|---|---|
| Кто | любой читатель | владелец и приглашённые пользователи после входа (P31) |
| Где | `radar.agpm.space`, режим «Агент» | `radar.agpm.space/editor` (P21) |
| Доказательная база | **только** immutable published knowledge release | весь допустимый complete full-text KX |
| Видит черновики | нет | да |
| Видит полный текст источника | нет, только опубликованные цитаты | да, в пределах допустимого |
| Лимиты | частота на адрес, длина вопроса, кэш, суточный потолок | лимиты пользователя, отдельный потолок |
| Аудит | обезличенный | пофамильный, с сохранением scope запроса |

**Наличие документа во внутреннем KX не означает его публичность.** Это правило проверяется
негативными тестами, а не полагается на аккуратность кода.

### 9.2. Требования к авторизации (P21)

1. **Серверная проверка на каждом privileged endpoint.** Не «скрытая кнопка», не проверка на клиенте,
   не догадка по маршруту.
2. **Scope в каждом запросе**, а не в сессии по умолчанию: endpoint принимает решение сам.
2а. **Учётные записи и роли (P31).** Минимум две роли: редактор — видит черновики и публикует;
   исследователь — задаёт вопросы по полному KX, но не публикует и не видит очередь одобрения.
   Приглашение, отзыв доступа и смена роли — привилегированные действия с аудитом.
3. **Защита от IDOR**: доступ к объекту проверяется по владельцу и scope, а не по знанию
   идентификатора.
4. **CSRF** для всех изменяющих операций редакторского режима.
5. **Раздельные лимиты** для public и editor.
6. **Аудит привилегированных действий**: кто, что, когда, с каким scope, какой объект.
7. **Утечки, которые обязаны быть закрыты негативными тестами**: черновик через public endpoint;
   полный текст через public endpoint; объект чужого scope по прямому идентификатору; повышение
   scope подменой параметра; ответ public-агента, содержащий доказательство из неопубликованного
   документа.

### 9.3. Контракты, подлежащие согласованию

- результат поиска (public и editor — разные формы);
- доказательный пакет;
- ответ агента и вердикт верификатора, включая форму abstention;
- публикуемый срез знаний;
- экспорт/импорт извлечения между плоскостями;
- отчёт покрытия и отчёт расхождений разметки.

Ни один из них сейчас не написан. До их согласования план не считается исполнимым.

---

## 10. Модель данных

### 10.1. Рубрикатор, понятия и wiki

| Сущность | Назначение |
|---|---|
| `topics` | Разделы **онтологии AgPM** из `agpm/wiki/` (P24): принципы, инфраструктура данных, четыре области практик, двухслойная ответственность, зрелость, организационный контекст, мультиагентный режим, сравнения. Параллельная рубрикация не вводится |
| `genres` | Жанр материала: исследование, релиз вендора, сделка, мнение, кейс, регулирование, новость |
| `material_topics`, `material_genres` | Разметка с реальной уверенностью, источником и версией промпта. Без срезов по количеству и без свалки по умолчанию |
| `concepts` | Страница-понятие. **Соответствует существующей странице `agpm/wiki/`** (P24): путь файла, раздел, статус, кто и когда одобрил. Новые понятия — только как предложенные дополнения |
| `concept_versions` | Immutable снимки авторского текста страницы. Структура разделов берётся из page conventions `SCHEMA.md`: Purpose, Core claims, Supporting sources, Tensions / Contradictions, Implications for AgPM model, Open questions |
| `concept_claims` | Разбор страницы на атомарные утверждения по разделам: нормативное, дескриптивное, имплементационное, открытый вопрос — разделение задано `SCHEMA.md` |
| `concept_evidence` | Связь утверждения страницы с claims и точными span'ами KX. **Ключевая новая работа**: существующие страницы написаны без привязки к span'ам |
| `concept_links` | Связи между страницами. Словарь — из `SCHEMA.md` без изменений: `supports`, `extends`, `constrains`, `contradicts`, `operationalizes`, `depends-on` |
| `wiki_snapshots` | **Новое (§6А.4).** Неизменяемый снимок состояния файловой wiki с хешем: без него `knowledge_release_id` в P22 неисполним |

### 10.2. Извлечение: exact и кандидаты разведены

| Сущность | Правило |
|---|---|
| `claims` | Утверждение с состоянием проверки (`proposed`/`accepted`/`rejected`/`superseded`), append-only журнал вердиктов |
| `claim_evidence` | **Только `exact`.** Триггер неизменяемого span'а сохраняется без изменений. Состояние `candidate` сюда не добавляется |
| `extraction_candidates` | **Новое.** Невыровненное или неподтверждённое извлечение: предложенная цитата, причина невыравнивания, статус, `run_id`, модель, хеш промпта, версия конфигурации. Переход в `claim_evidence` только через явную валидацию, порождающую exact-span |
| `processing_runs` | Есть. Добавить хеш промпта отдельно от параметров, счётчик попыток, текст ошибки, ограниченный переход статуса |
| `metrics` | Заполняется только детерминированным разбором проверенного span'а |

### 10.3. Переводы и публикуемые цитаты

| Сущность | Назначение |
|---|---|
| `quote_translations` | Перевод span'а: язык оригинала, текст, автор, хеш промпта, состояние, результат сверки инвариантов |
| `published_quotes` | Опубликованная выдержка: span оригинала, перевод, длина, атрибуция, ссылка, отметка автоматической публикации (P19) или ручного решения |
| `publication_quarantine` | Элемент, не прошедший fail-closed условия: что именно не прошло, когда, что нужно для повторной попытки |
| `source_publication_policy` | Формулировка атрибуции на источник и, при необходимости, отклонение от общего предела длины. По умолчанию действует единое правило P32; особых режимов по типу источника нет (P34) |

### 10.4. Независимость источников (D13)

| Сущность | Назначение |
|---|---|
| `source_families` | Группа изданий и доменов с общим владельцем, редакцией или каналом синдикации |
| `document_source_family` | Принадлежность документа семье |
| `content_duplicate_clusters` | Кластеры содержательных дубликатов: перепечатка, синдикация, общий пресс-релиз, общий первоисточник |
| `duplicate_evidence` | Основание кластеризации: совпадение по хешу, по шинглам, по цитируемому первоисточнику |

Правила, обязательные к применению в scoring, graph, допуске идей и gold-тестах:

- два документа одной `source_family` дают **одно** подтверждение;
- документы одного `content_duplicate_cluster` дают **одно** подтверждение независимо от семьи;
- пресс-релиз и его перепечатки — один источник; аналитический разбор того же релиза — отдельный,
  если содержит собственные данные;
- признак независимости хранится вместе с оценкой, а не вычисляется на лету.

### 10.5. Идеи и рейтинги (P13, P22)

| Сущность | Назначение |
|---|---|
| `idea_candidates` | Идея-кандидат из группы claims: формулировка, опорные утверждения, прогон-источник, статус, решение владельца, причина отказа |
| `ideas`, `idea_evidence` | Принятые идеи с происхождением |
| `scoring_models` | Immutable определение версии оценки: измерения, признаки, веса, описание |
| `material_scores` | **Двухуровневая оценка материала (P22).** Обязательные поля: `knowledge_release_id`, версия рубрики, версия промпта, версия модели, объяснение, ссылки на evidence spans |
| `score_events` | Каждый re-score — отдельное версионированное событие. Исторические оценки не переписываются |
| `score_explanations` | Какие доказательства и с каким вкладом повлияли. Без объяснения оценка не сохраняется |

**Два уровня оценки, которые нельзя смешивать:**

| Уровень | Относительно чего | Измерения |
|---|---|---|
| Концептуальный | **конкретного immutable knowledge release** wiki | применимость к концепции агентного управления; новизна относительно того, что wiki уже утверждает |
| Источниковый | **полного KX** | новизна источника; независимость доказательств; duplicate/source-family статус; 90-дневный моментум темы |

Измерения **не сводятся в один непрозрачный балл**. Читателю и владельцу показываются раздельно, с
объяснением по каждому.

**Порождение идей (P13).** Кандидат собирается из группы claims, повторяющихся или противоречащих
друг другу. Требования: не менее двух опорных claims из **разных source-family** (§10.4); в
формулировке нет чисел и цитат — они подставляются рендерером; дубликаты и переформулировки прежних
идей отсеиваются до показа владельцу; отклонённая идея запоминается и не предлагается повторно, пока
не появились новые доказательства.

### 10.6. Граф v1 (P23)

**Приоритет 1 — смысловой слой вокруг wiki:** узлы `concept`, `idea`, `claim`; рёбра между ними.
Словарь рёбер — существующий из `agpm/SCHEMA.md` (P24): `supports`, `extends`, `constrains`,
`contradicts`, `operationalizes`, `depends-on`. Собственный словарь не вводится.
**Приоритет 2 — доказательная трасса:** `claim → exact evidence → document → source family`.

Компании, продукты, роли и временная эволюция остаются расширяемыми представлениями поверх тех же
данных и **не определяют UX первой версии**. `entities`, `entity_aliases`, `entity_merges` и
`relations` сохраняются в схеме как задел.

| Сущность | Назначение |
|---|---|
| `graph_snapshots` | Immutable параметризованный экспорт: фильтр, версия, счётчики узлов и рёбер, SHA-256 |

Окрестности — рекурсивные CTE с жёстким серверным лимитом (ориентир ≤300 узлов / ≤1 000 рёбер).

### 10.7. Публикация, ответы и приёмка

| Сущность | Назначение |
|---|---|
| `knowledge_releases` | Immutable срез: состав, счётчики, хеш состояния, момент публикации; активный указатель переключается атомарно. **Физическая форма** — предмет ADR-0006; чтобы у него был вход, варианты названы: **(а)** SQLite-релиз по образцу content release V2, отдаётся расширением `v2/apps/api` — максимум переиспользования, но публичный API V2 перестаёт быть только про выпуски; **(б)** отдельная схема в PostgreSQL и новый read-only сервис — чище по границам, но новый компонент; **(в)** статический экспорт плюс поисковый индекс — дёшево, но диалоговый агент всё равно потребует сервиса. Кандидат по умолчанию — (а) |
| `review_queue` | Очередь одобрения авторского слоя |
| `answer_runs`, `answer_citations`, `verifier_verdicts` | Аудит: вопрос, scope, параметры поиска, доказательства, модель, хеш промпта, сырой вывод, вердикт, финальный текст, стоимость. **Хранение бессрочное (P33)**, отсюда обязательны: политика конфиденциальности как условие запуска, ограничение доступа к архиву вопросов ролью, механизм удаления по запросу читателя |
| `wiki_snapshots` | **Снимок состояния файловой wiki с контрольной суммой (P27).** Снимается перед сборкой каждого knowledge release; на него ссылается `knowledge_release_id` в оценках |
| `store_reconciliation_reports` | **Сверка файлового хранилища с KX (P28)**: что есть только в одном, где тексты различаются, какая версия новее |
| `eval_sets`, `eval_questions`, `eval_gold_evidence`, `eval_runs` | Gold-наборы и измерения по каждому классу проверок (§13.1) |
| `egress_audit` | **Новое (P18).** Каждый вызов внешней модели: какой документ или фрагмент передан, какой объём, какому провайдеру, какой моделью, с каким прогоном связан |

---

## 11. Развилки: решения

### 11.1. Извлечение документов

Trafilatura подтверждена (в production, `canonical-v4`, плюс BeautifulSoup, pypdf и разборы под
источник: Telegram, Reddit JSON, Pandaily Remix, JSON-LD, Contentful). Docling отклонён на этой фазе:
не закрывает текущих пробелов (88 строк из ~2 400). Возврат — по измеренному дефекту на PDF,
отдельным ADR, в отдельном юните.

### 11.2. Immutability

Immutable источник; результат обработки не изменяем после терминального статуса; решения append-only,
отмена — новая строка; производное пересобираемо, но каждая строка несёт
`(processor, processor_version, params_sha256, model_id, run_id)`.

### 11.3. Механизм извлечения

**Исходная гипотеза сохраняется как проверяемая, а не как догма:** Trafilatura/Docling → immutable
versions → LangExtract adapter → PostgreSQL FTS + pgvector → evidence-grounded answer/verifier →
SQL graph + Cytoscape.js. Любая замена допустима через ADR и измерение на gold-наборах.

Текущее решение внутри гипотезы: LangExtract не берётся в runtime фаз 1-2; берётся смешанный
конвейер — модель возвращает **дословную цитату**, смещения ищем сами детерминированно, только
`exact` проходит в `claim_evidence`, остальное — в `extraction_candidates`. `ExtractionAdapter`
оставляет LangExtract возможной второй реализацией, оцениваемой на том же gold-наборе.

**Процедура расхождения кросс-проверки.** Обе модели подтверждают span → `exact`. Вторая не
подтверждает → кандидат с причиной `cross_check_failed`. Расхождение в числе, дате или единице
внутри подтверждённого span'а → блокирующая ошибка и отдельная категория в очереди, а не понижение
статуса. Кросс-проверка включается после первого замера на gold-наборе.

**Маршрутизация по длине документа** (замер: в среднем 13 990 символов, минимум 215, максимум
193 217): очень короткие дают цитату и карточку, но не набор утверждений; обычные — один вызов на
документ; длинные — почанково со сведением. Заявленное окно MiniMax-M3 в 1 млн токенов взято из
конфигурации и **на практике не проверялось**; качество на длинном контексте не измерял никто,
поэтому порог подтверждается на gold-наборе.

### 11.4. Поиск

Два разных поиска. **Внутренний** — по полным текстам KX; лексический слой работает уже сегодня на
готовых `search_ru`/`search_en`. **Публичный** — по нашему тексту: понятия, выжимки, идеи,
опубликованные цитаты.

Источника эмбеддингов нет: ни в Hermes, ни в OpenClaw не настроено ни одной модели эмбеддингов.
Варианты — сторонний эндпоинт (третья модель в смысле P9), локальная модель (единственное место с
настоящим локальным счётом: 1,5-2 GiB, вывод на процессоре) или отказ от векторов. Решение — после
замера лексики на gold-наборе.

### 11.5. Классы доступа к источникам

Формулировка «юридическая сторона закрыта» **отменена**: P11 — продуктовое решение владельца, а не
юридическое заключение. Классы разделены и обрабатываются по-разному:

| Класс | Автоматическая лестница | Замечание |
|---|---|---|
| Публично доступная страница | да | обычный путь |
| Ограничение robots | да | сигнал маршрутизации, не терминальное состояние (P11) |
| Веб-архив | да | ссылка ведёт на снимок с датой |
| Артефакт оператора и локальный импорт | да | провенанс: кто и когда предоставил |
| Стандарты и авторитетные материалы | да | ограничений нет (P34): хранение и цитирование согласованы |
| Требующее учётных данных | по мере настройки | лестница использует только те учётные данные, которые ей заданы; сама их не подбирает — это техническое свойство, а не разрешительная процедура |

Провенанс сохраняется всегда и для всех классов — он нужен повторным попыткам и верификатору.
Операционный риск — блокировка адреса; управляется темпом, приоритетом архива и паузами после
403/429.

### 11.6. Подсистема полнотекстовой добычи

Полный текст есть у 5 979 из 8 313 документов (71,9%). Ступени: обычный запрос → браузерные
заголовки → запрос при ограничении robots → разбор под источник → рендеринг в браузере → веб-архив
→ артефакт оператора. Тип источника — техническая запись способа.

Подсистемой это делают: профиль хоста (порядок ступеней, темп, заголовки), состояние стратегии на
документ, автоматическая эскалация вместо терминального отказа, осмысленные терминальные состояния,
очередь пробелов с владельцем действия.

Главный рычаг существует как настройка: `RADAR_KX_RESPECT_ROBOTS` (`config.py:80`, по умолчанию
`True`), читается в `fetcher.py:225`. Снятие гейта открывает разбор ~1 745 документов Reddit.
Переключать глобально не следует — настройка должна жить в профиле хоста.

Недостающие инструменты: клиент веб-архива и рендеринг в браузере (внутри юнита сбора невозможен,
D7; нужен отдельный юнит).

### 11.7. Размещение обработки и egress (P18)

| Работа | Где | Основание |
|---|---|---|
| Разбор документов | Local Ru, профиль Hermes + оркестратор | локальность к БД, единая точка постоянного хранения, утверждённый шлюз моделей. **Не** «текст не должен уезжать» — это основание отменено P18 |
| Авторская переработка | control-хост | работает по извлечённому, полный текст не нужен |
| Оценка материалов (P22) | control-хост, Project Manager | уровень 1 по опубликованному срезу, уровень 2 — через authenticated research API |
| Публичный и редакторский Q&A | Local Ru, профили Hermes | читают срез или KX по scope |

**Контракт egress (P18):**

1. Разрешены **только** утверждённые эндпоинты GLM и MiniMax. Всё остальное — deny by default.
2. Ограничение применяется на уровне юнита профиля Hermes, а не только конфигурацией приложения.
3. **Минимизация контекста**: передаётся минимально необходимый фрагмент, а не документ целиком, если
   задача этого не требует. Правило фиксируется на каждый тип прогона.
4. **Аудит каждого вызова** в `egress_audit`: что передано, какого объёма, куда, какой моделью, в
   рамках какого прогона.
5. **Запрет retention и обучения** там, где это поддерживается API или условиями провайдера;
   фактическая поддержка проверяется и записывается, а не предполагается.
6. Оркестратор остаётся без интернета: только loopback к Hermes и unix-сокет PostgreSQL.

### 11.8. Профили Hermes, а не агенты в общем шлюзе

Профиль в Hermes — фактически отдельный инстанс: свой `HERMES_HOME`, конфиг, состояние, ключи, порт и
юнит из шаблона `hermes-gateway@.service`. Общими остаются код и venv.

Отдельные профили нужны из-за: разной сетевой политики; разных прав (общий шлюз работает от root);
изоляции отказа; изоляции состояния; разного жизненного цикла. Прецедент на этом же хосте — NRD с
`nrd-intake` и `nrd-analysis`.

Переиспользовать у NRD: собственный runtime (общий venv резолвится через `/root` и падает под
`ProtectHome`), патч allowlist моделей (Hermes принимает произвольный идентификатор модели даже при
настроенном `model_routes`), root-owned env 0600, fail-closed верификатор контракта.

Контуров на Local Ru: общий шлюз (существует, не трогаем), профиль извлечения, профиль публичного
агента, профиль редакторского Q&A.

### 11.9. Граница интерфейса и Hermes

Читатель работает с интерфейсом Radar; Hermes не виден и публичного адреса не имеет. Поиск, сборка
доказательного пакета, лимиты, детерминированная сборка ответа и верификация — в коде Radar. Модель
возвращает только структуру и ссылки. Отказ Hermes не ломает базу знаний: wiki, поиск, граф и
рейтинги читаются из среза и работают без модели.

**Требования безопасности агента:** вопрос читателя — данные, а не инструкция; содержимое чужих
статей в доказательном пакете — тоже данные, и вывод, следующий инструкции из цитаты, отвергается.
Оба требования закрываются негативными тестами (§13.1).

---

### 11.10. Разбор референса pimenov.ai — что заимствуем в интерфейсе

Изучены разделы `/`, `/graph/`, `/topics/`, `/start-here/`, `/knowledge/`.

**Заимствуем как есть**

| Приём | Как у них | Как у нас |
|---|---|---|
| **Аналитика под графом** | Под облаком — карточки: связность 100%, сирот 0, топ-12 центральных узлов, срезы по разделам, топ-18 тем, примеры связей | Самая ценная идея на сайте. Превращает картинку в диагностический инструмент. У нас те же карточки, но про качество базы: утверждений без доказательства, понятий-сирот, тем без свежих материалов, противоречий, источников-одиночек |
| Фильтр по типу узла над графом | Все / Статьи / Знания / Блог / Форматы / Сброс | Все / Понятия / Идеи / Утверждения / Материалы / Источники |
| Боковая панель по клику, без перехода | Тип, теги, ближайшие переходы | Понятие, его core claims, цитаты с доказательствами, соседи по типу связи |
| Счётчики у тем | «476 материалов» рядом с маршрутом | Уже заложено для владельца (P17). Показать и читателю: вес темы виден сразу |
| Точка входа по ситуации, а не по роли | «Начинаю с нуля», «Ищу тему», «Есть задача», «Хочу по направлениям» | Сильнее ролевой сегментации: человек знает свою ситуацию, но не всегда относит себя к роли. У нас: «первый раз здесь», «ищу конкретный факт», «разобраться в теме», «слежу за изменениями» |
| Переключатель тем | Светлая / тёмная / киберпанк | В Radar тема уже есть — не забыть распространить на базу знаний |

**Заимствуем и усиливаем — за счёт того, чего у них нет**

| Приём | Их версия | Наша |
|---|---|---|
| Карточка материала | Обложка, заголовок, описание, теги | Плюс источник, дата, периметр, двухуровневая оценка (P22) и число подтверждающих цитат. **Вместо обложки — самая сильная цитата из материала**: честнее и полезнее картинки |
| Топ центральных узлов | Про связность структуры сайта | Про предметную область: какие понятия чаще всего попадают в цепочки доказательств. Тот же интерфейс, другой смысл |
| Смысловые маршруты | 8 маршрутов по тегам | Хребет — онтология AgPM. Плюс маршрут, которого нет ни у кого: **«где источники расходятся»** — прямо следует из `Tensions / Contradictions` в SCHEMA.md и связи `contradicts`. Для читателя это самый интересный вход |

**Не заимствуем**

| Приём | Почему |
|---|---|
| Обзорный граф всего сайта | 688 узлов и 109 связей «из 178 704 сырых» — красиво, но работать по нему нельзя. У нас граф всегда **от чего-то**: от понятия, идеи или материала, с окрестностью и серверным лимитом (§10.6). Общая карта области допустима отдельной картинкой, но не как основной инструмент |
| Пагинация на 22 страницы | Для растущего корпуса плохой способ. Нужен фильтр и поиск, а не листалка |
| Обложки материалов | Генерировать картинки к чужим статьям — работа без ценности |
| Плашки «скоро» в интерфейсе | У них «Перезагрузка скоро», «Скоро: образовательная программа». Мы решили, что состояния «текст готовится» на публичных страницах не будет (P19) |

**Чего у них нет, и это наше отличие.** Ни одной точной цитаты со ссылкой на место в источнике; ни
отметки, откуда взято утверждение; ни противоречий между источниками; ни честного отказа «в базе
этого нет»; ни оценок с объяснением. Они делают контент-сайт, мы — доказательную базу. Заимствуем
форму навигации, но не перенимаем отказ от доказательности.

---

## 12. Resource budget

Объёмы — замер production 2026-08-22. Пересчёт токенов при ~2,8 символа на токен для смешанного
ru/en.

| Операция | Объём | Токены за проход |
|---|---|---|
| Вертикальный срез (20-30 документов) | ~0,4 M символов | **~0,15 M вх.** |
| Извлечение по периметру | 3,85 M символов, 275 документов | **~1,4 M вх.** / 0,15-0,4 M вых. |
| Извлечение по всему корпусу | 59,2 M символов, 6 489 версий | **~21 M вх.** / 3-10 M вых. |
| Эмбеддинги периметра | 1 098 чанков | ~1,4 M |
| Эмбеддинги всего корпуса | 19 010 чанков | ~21 M |
| Перевод цитат | 10-20 цитат на документ | ~0,1-0,3 M на 275 документов |
| Привязка и дописывание страниц wiki | ~60 существующих страниц плюс предложенные дополнения (P24) | ~0,5-1,5 M |
| Оценка материалов (P22) | 4-6 материалов в сутки × два уровня | десятки тысяч токенов в сутки |
| Ежедневный прирост | 4-6 документов, ~15 чанков | десятки тысяч токенов |

Стоимость повторяется при каждой смене промпта или модели; реалистично 3-5 проходов на доводку.
Минимизация контекста (P18) снижает передаваемый объём и должна учитываться при пересчёте.

**Хранение.** Рост сырья ~4 MB stored в сутки. Эмбеддинги при 1024 измерениях: периметр 4,5 MB, весь
корпус 78 MB, с HNSW 150-200 MB. БД 688 MB, свободно 50 GB. Хранение не ограничивает.

**Ресурсы.** Local Ru: 4 vCPU, 7 936 MB RAM, swap 8 GB, 50 GB свободно. Локального вывода моделей
нет — обе модели облачные, Hermes это HTTP-клиент. Ежедневная нагрузка на Local Ru — 30-60 вызовов,
меньше, чем уже делает сбор KX (~108 документов в сутки). Память — пункт чеклиста, не риск.

**Что действительно ограничивает:** лимиты провайдера и стенные часы на пакетных прогонах (275
документов ≈ 1 000-1 500 вызовов; при реальных 30-120 с на вызов с большим контекстом это **часы, а
не полчаса**); трафик читателей после запуска; 4 ядра при возможном локальном эмбеддере.

**Латентность (цели, подлежат замеру).** Публичный поиск p95 < 300 ms; внутренний поиск < 800 ms;
public-ответ < 10 s; editor research-ответ < 20 s; ежедневная синхронизация < 5 min без сетевых
fetch.

---

## 13. Фазы

### 13.1. Вертикальный срез — предусловие любого массового прогона

**До обработки всего периметра выполняется внутренний вертикальный срез на 20-30 репрезентативных
документах: от ingest до knowledge release, через все слои.** Только после прохождения порогов срез
масштабируется на 275+.

Выборка формируется осознанно: ru и en, разные жанры, разные семьи источников, короткий и длинный
документ, документ из веб-архива, документ из артефакта оператора, документ с числовыми данными,
пара заведомых перепечаток одного первоисточника.

Для каждого класса — свой gold-набор и **измеримый порог**, а не «зафиксировать результат»:

| Класс проверки | Что измеряется | Порог |
|---|---|---|
| Retrieval | Recall@10, MRR, p95 latency | **измеряется, планка ставится владельцем после (P29)** |
| Доля отказов | сколько вопросов закрыто доказательствами, сколько ушло в abstention | **измеряется, планка ставится владельцем после (P29)** |
| Extraction / claim binding | доля фактических clause с привязкой к accepted claim; доля exact от предложенных цитат | **измеряется, планка ставится владельцем после (P29)** |
| Числа и единицы | расхождение значения, единицы или периода между ответом и span'ом | **ноль** |
| Переводы | расхождение инвариантов (числа, даты, единицы, имена) | **ноль** |
| Независимость источников | доля утверждений, усиленных документами одной семьи | **ноль** |
| Abstention | доля вопросов без основания, на которые дан ответ вместо отказа | **ноль** |
| Prompt injection из вопроса | доля запросов, где модель следует инструкции из вопроса | **ноль** |
| Prompt injection из текста источника | доля ответов, где модель следует инструкции из цитаты | **ноль** |
| Авторизация public/editor | утечка черновика, полного текста или чужого объекта через public endpoint | **ноль** |

### 13.1а. Границы вертикального среза

Без этого срез расползается: он требует «все слои», а слои по таблице фазы 2 строятся после него.
Разведено явно.

| Слой | В срезе 2.1 | Индустриализуется |
|---|---|---|
| Добыча и полный текст | берутся уже имеющиеся документы периметра | 2.2, 2.3 |
| Извлечение claims и span'ы | **строится прототипом**: рабочий конвейер на 20-30 документах | 2.6 |
| Source-family и дубликаты | **строится прототипом**: ручная разметка семей для выборки | 2.4 |
| Привязка утверждений wiki | **строится прототипом** на 3-5 страницах | 2.5 |
| Переводы цитат | **строится прототипом** | 2.8 |
| Оценка материала | **строится прототипом**, оба уровня | 2.10 |
| Граф | **имитируется**: достаточно рёбер в БД, визуализация не нужна | 2.11 |
| Публикация | **имитируется**: собирается срез-черновик в файл, без атомарного переключения и без публичной поверхности | 3.1 |

**Bootstrap `knowledge_release_id` (P22).** До первой публикации ни одного knowledge release не
существует, а поле обязательно. Правило: до 3.1 оценки ссылаются на `wiki_snapshots.snapshot_id`
(P27), и этот же идентификатор становится `knowledge_release_id` первого настоящего релиза. Оценки,
проставленные до 3.1, не переписываются — они уже привязаны к корректному снимку.

**Выборка и веб-архив.** Требование «документ из веб-архива» в выборке выполняется одним из двух
способов: либо к моменту 2.1 восстановлен архивный провенанс четырёх документов из 1.3, либо
позиция заменяется на «документ, добытый не обычным запросом» — операторский артефакт или fetch под
браузерными заголовками. Ждать клиента веб-архива из 2.3 срез не должен.

**Порядок по P29:** срез прогоняется без заранее назначенной планки для retrieval, доли отказов и
claim binding; результаты предъявляются владельцу; **планка ставится до масштабирования на 275+, а
не после.** Иначе планкой становится то, что получилось. Нулевые пороги в таблице обсуждению не
подлежат.

### 13.2. Фаза 1 — согласование контрактов и безопасный фундамент

Наружу ничего не выходит. Работы, не зависящие от несогласованных контрактов, можно начинать сразу.

| Срез | Содержание | Зависит от контрактов |
|---|---|---|
| **1.1 Corpus-membership contract** (§6, §6А.4) | Объяснить 284 / 280 / 277 / 275 воспроизводимыми запросами; включить в разбор `agpm-radar/data/materials.jsonl` (8 446 строк) и `data/source-fulltext/` (75 файлов) — учёт ведётся минимум в четырёх местах; **добавить корпус канона (1.6) как отдельный класс членства**; зафиксировать единицы счёта, канонизацию, дедупликацию, current vs historical, источники истины; **зафиксировать в контракте уже принятое P28**, а не решать заново. Числа 277 и 275 живут в production — нужен санкционированный read-only доступ либо локальная реконструкция релиза; способ выбирается в начале среза и записывается | **Нет.** Первая работа, предусловие остального |
| **1.2 ADR** | `0004` доказательства, claim binding и abstention; `0005` плоскости, egress-контракт и оркестратор; `0006` публикация, два scope, авторизация редактора **и версионирование wiki (§6А.4)**; `0007` независимость источников и source-family; `0008` отношение файловой wiki и KX: роли, направление синхронизации, что является источником истины для чего | — |
| **1.3 Миграция 003** | Таксономия ступеней добычи, таблица провенанс-коррекций (D12), backfill 25 версий, `egress_audit`, тесты. Runbook: остановка `radar-kx-ingest.timer` (30-минутный интервал, иначе падение по `require_schema`), backup вне окна backup-таймера, миграция, проверка версии, переключение указателя, verify, возврат таймеров | Нет |
| **1.4 Внутренний поиск** | `radar_kx search` с RRF по ru/en, снипеты из точных offsets, разделение текущего и исторического периметра, `coverage-report`. Гейт: «не менее 993 и 633» либо сверка со снимком корпуса — точные числа невоспроизводимы на растущем корпусе | Нет |
| **1.6 Корпус канона** (P25) | Загрузка канона и стандартов в KX отдельным корпусом. Определение среза ниже — без него он неисполним | 1.3 (нужен `import-artifact` и `source_publication_policy`) |
| **1.5 Инвентаризация wiki** (P24) | Прочитать все 93 + 204 файла; составить реестр страниц с разделами по конвенциям `SCHEMA.md`; выделить атомарные утверждения; отметить, какие уже ссылаются на источники, а какие нет. **Read-only, ничего не меняет в `knowledge/`** | Нет |

**Определение среза 1.3.** В редакции 5 подробный состав был сжат до строки таблицы, и часть
deliverables потерялась. Восстановлено:

1. `kx/sql/003_*.sql`: таксономия ступеней добычи (`network_browser_headers`, `browser_render`,
   `web_archive` в дополнение к существующим), **append-only таблица провенанс-коррекций** (D12),
   `source_publication_policy`, `egress_audit`, `wiki_snapshots`, `store_reconciliation_reports`,
   `corpus_imports.source_kind`, `GRANT`, bump `schema_version` до 3.
2. **Восстановить утерянный путь импорта**: `kx/src/radar_kx/artifact_import.py` и команда
   `radar_kx import-artifact --manifest ...`. Он нужен и как ступень 7 лестницы (§11.6), и как
   единственный инструмент среза 1.6. Утеря при переписывании — регрессия, а не решение.
3. **Состав полей провенанса** (восстановлен): `source_access_method` со значениями
   `http_default`, `browser_headers`, `robots_override`, `browser_render`, `web_archive`,
   `operator_file`, `local_import`; `archive_used`, `archive_url`, `archive_captured_at`;
   `browser_used`; `manual_review_required`; `provided_by`, `provided_at`,
   `original_url`, `notes`.
4. **Backfill провенанса для 25 существующих версий** из
   `/root/.openclaw/workspace/reports/radar-kx-fulltext-close-2026-08-22.md` и переданного владельцем
   HTML-артефакта. Отдельно: **не менее четырёх документов** (adopt.ai, McKinsey и другие) фактически
   взяты из веб-архива, но записаны без архивного URL и даты снимка. До их восстановления правило 19
   (§8.6) для этих документов неисполнимо, и **публичные цитаты из них блокируются** — это
   принимается как временное ограничение, а не как ошибка.
5. Запись в таблицу коррекций, что версии Appian и Futurum получены ступенью
   `network_browser_headers` (D9). Новые версии не создаются.
6. Запрет на уровне кода записывать сетевой запрос как `operator_artifact`.
7. Тесты, включая правку `kx/tests/test_schema_contract.py` (ассертит `'2'::jsonb`) и негативные:
   артефакт без провенанса отклоняется, метаданные не становятся полным текстом, повторный импорт
   идемпотентен.

**Определение среза 1.6.** Заголовка недостаточно, поэтому фиксируется здесь.

1. **Что фактически доступно.** Проверено по `knowledge/agpm/raw/`: полные тексты есть у White Paper
   v1.0 и v1.2, Манифеста v1/v2/v3, обоснования онтологии, AgPMBoK v0.7, ГОСТ Р 72514-2026, NGMN,
   Sber, AWS и двух статей arXiv. ISO 21502 и PMBOK 8 лежат выдержками, PMI AI PPPM в `raw/` пока
   нет — только заметка в `wiki/sources/`. Это вопрос наличия файлов, а не разрешений (P34):
   загружаем всё, что есть, помечаем уровень полноты, и если полные тексты появятся — догружаем тем
   же путём.
2. **Инструмент** — `radar_kx import-artifact` (восстанавливается в 1.3). Сетевой fetch неприменим.
3. **Идентичность.** `documents.canonical_url` — `NOT NULL UNIQUE`, `document_id = sha256(canonical_url)`.
   Локальные документы без веб-адреса получают синтетический канонический URL по схеме
   `agpm-canon:/<относительный-путь-в-raw>`; схема фиксируется в 003 и не пересекается с `http(s)`.
4. **Форматы.** Разбираются только `raw/*.md` — они уже извлечены и неизменяемы по соглашению
   `SCHEMA.md`. DOCX и PDF из `raw/originals/` **не разбираются** этим срезом: текущий парсер не
   поддерживает DOCX, а Docling отклонён (§11.1). Если полнота выдержки окажется недостаточной,
   расширение формата — отдельное решение.
5. **«Отдельный корпус» в терминах схемы** — новая строка `corpus_imports` с собственным
   `source_name` и `source_kind = 'canon_import'`; периметр выпусков он не затрагивает и в
   знаменатели покрытия по материалам радара не входит.

**Требует подтверждения владельца:** применение миграции к production и переключение
`/opt/radar-kx/current`. Внутри среза не делается.

### 13.3. Фаза 2 — вертикальный срез и содержимое

| Срез | Содержание | Зависимости |
|---|---|---|
| **2.0** Профиль Hermes + оркестратор | Профиль по образцу NRD с egress-allowlist (P18); оркестратор без интернета, пакетный и дежурный режимы | 1.2, 1.3 |
| **2.1** Вертикальный срез 20-30 документов | §13.1 целиком, включая gold-наборы и пороги | 1.1, 2.0 |
| **2.2** Самопополняемый периметр | Опрос `active.json` → экспорт → импорт → очередь новых URL. Без хука в путь публикации V2. **Встраивается в существующий контур** `runs/` → `wiki/daily/` → `wiki/monthly/` → основная wiki, а не подменяет его (§6А.2) | 1.1 |
| **2.3** Подсистема добычи | Профили хостов, эскалация, веб-архив, классы доступа (§11.5) | 1.3 |
| **2.3а** Рендеринг в браузере | Отдельный юнит без `MemoryDenyWriteExecute` (D7), свой пользователь, своя сетевая политика. Ступень 5 лестницы (§11.6). Строится **по измеренной необходимости**: когда очередь пробелов 2.3 покажет случаи «берётся только браузером». В редакции 5 срез был утерян — эскалация 2.3 упиралась в несуществующую ступень | 2.3 |
| **2.4** Source-family и дубликаты | Кластеры, правила синдикации, признак независимости | 1.1 |
| **2.5** Привязка wiki к доказательствам (P24) | Импорт существующих страниц как `concepts`/`concept_versions`; разбор на атомарные утверждения по разделам `SCHEMA.md`; **связывание каждого утверждения с claims и точными span'ами KX** — как из материалов выпусков, так и из корпуса канона (1.6); отчёт «утверждения без доказательства». Главная работа фазы | 1.5, 1.6, 2.0, 2.6 |
| **2.4а** Отчёт сверки хранилищ (P28) | Регулярное сравнение `agpm-radar/data/source-fulltext/` и KX: что есть только в одном, где расходятся тексты, какая версия новее | 1.1 |
| **2.5а** Снимки wiki (P27) | Снимок состояния `knowledge/` с контрольной суммой перед сборкой каждого knowledge release; на него ссылается `knowledge_release_id` оценок. Git не вводится | 1.2 (ADR-0006) |
| **2.5б** Дополнения к понятиям | Машина предлагает **новые** понятия и разделы с числом подтверждающих материалов; владелец решает (P17). Существующая структура при этом не переписывается | 2.5, 2.6 |
| **2.5в** Переразметка материалов | Темы из онтологии AgPM, периметр, жанр по существующей карте жанров; по полному тексту, без срезов, с настоящей уверенностью | 1.1, 2.0 |
| **2.6** Извлечение claims | Claims, exact spans, `extraction_candidates`, метрики | 2.1, 2.4 |
| **2.7** Тексты понятий | Авторские черновики, связи | 2.5, 2.6 |
| **2.8** Переводы и автопубликация | Перевод при span'е, сверка инвариантов, автоматическая публикация структурного слоя при fail-closed (P19), quarantine | 2.6 |
| **2.9** Идеи-кандидаты | Порождение из claims с гейтом независимости источников | 2.6, 2.4 |
| **2.10** Двухуровневая оценка (P22) | `scoring_models`, `material_scores` с `knowledge_release_id`, `score_events`, объяснения. Интеграция с разбором материала в Project Manager | 2.6, 2.9, 2.4 |
| **2.11** Граф v1 | Приоритет 1 — concepts/ideas/claims; приоритет 2 — доказательная трасса; `graph_snapshots` | 2.6, 2.9 |
| **2.12** Режим редактора | Очередь авторских черновиков, решения, доказательства рядом. Первая часть будущего интерфейса | 2.7, 2.9 |
| **2.13** Отчёт расхождений разметки (P16) | Где старые 11 рубрик и новая таксономия расходятся, на живых данных | 2.5в |
| **2.14** Внутренний research Q&A | Строгий режим по полному KX, claim binding, abstention | 2.6, 2.1 |
| **2.15** Определитель языка | Замена счётчика кириллицы | — |
| **2.16** pgvector | Только если 2.1 показал нехватку лексики | 2.1 |

**Календарь.** Фаза 2 в месяц не помещается: двадцать срезов, часть зависит от ручной разметки
gold-наборов и от пропускной способности одобрения. Реалистично **8-12 недель** с учётом
вертикального среза. Открытие существующей wiki (P24) объём скорее **сокращает**, чем увеличивает:
таксономию, конвенции, словарь связей и значительную часть авторских текстов писать не нужно —
нужно связать написанное с доказательствами.

### 13.4. Фаза 3 — публикация и публичный запуск

| Срез | Содержание | Gate входа |
|---|---|---|
| **3.1** Контракт публикации | `knowledge_releases`, атомарное переключение, откат, реконсиляция | ADR-0006 |
| **3.2** KB API и авторизация | Public и editor scopes, серверная проверка на каждом privileged endpoint, CSRF, лимиты, аудит (§9). **Ненаписанный компонент, отдельная позиция объёма** | 3.1 |
| **3.3** Интерфейс | Третий режим `агент` в `v2/apps/web/index.html` и `app.mjs`; `/editor` после входа; Claude Design на решениях Radar. Заимствования из референса — §11.10: аналитика под графом, фильтры типов узлов, боковая панель, точка входа по ситуации, маршрут «где источники расходятся» | 3.2 |
| **3.4** Публичный агент | Профиль Hermes, диалог по опубликованному срезу, лимиты и кэш на стороне Radar, защита от инъекций | 2.14 |
| **3.5** Security acceptance | Негативные тесты §9.2 полностью зелёные | 3.2, 3.3, 3.4 |
| **3.6** Запуск | Публикация первого среза | все предыдущие |

---

## 14. Assumptions и discovery

### 14.1. Закрыто замером 2026-08-22

| # | Было допущением | Факт |
|---|---|---|
| A1 | ресурсы Local Ru | 4 vCPU, 7 936 MB RAM, swap 8 GB, 50 GB свободно |
| A4 | объём чанков | 19 010 по корпусу, 1 098 по периметру; 59,2 M и 3,85 M символов |
| A6 | провенанс двух документов 77-го выпуска | подтверждён (D9) |
| A7 | версия схемы в production | `schema_version = 2`; оба constraint'а содержат `operator_artifact` |

### 14.2. Действующие допущения

| # | Допущение | Что ломается, если неверно |
|---|---|---|
| ~~A2~~ | **ЗАКРЫТО решением P30**: тексты передаются независимо от наличия гарантий retention/training | Отключение запрашивается там, где поддерживается, но не является условием работы |
| A3 | Периметр растёт на 4-6 документов в сутки, корпус на ~108 | Смещаются оценки §12 |
| A5 | Ритм одобрения 15-30 минут ежедневно достаточен после снятия структурного слоя (P19) | Проверяется в 2.12 на реальном объёме |
| A8 | База знаний размещается на `radar.agpm.space` разделом, редактор — на `/editor` | Меняется конфигурация Caddy и §9 |
| A9 | Заявленное окно контекста MiniMax-M3 в 1 млн токенов достижимо с приемлемым качеством | Меняется маршрутизация §11.3 и оценка числа вызовов |
| A10 | Канонизация URL в Legacy-пайплайне, в `agpm-radar/data/materials.jsonl` и в KX совпадает | Ломается corpus-membership contract (§6, §6А.4) |
| A11 | Существующие страницы `agpm/wiki/` можно разобрать на атомарные утверждения автоматически с приемлемой точностью | Привязка к доказательствам (2.5) становится ручной работой; объём фазы 2 растёт |
| ~~A12~~ | **ЗАКРЫТО решением P25**: канон и стандарты загружаются в KX отдельным корпусом (срез 1.6) | Остаётся проверить полноту покрытия после загрузки: отчёт «утверждения без доказательства» из 2.5 покажет фактическую цифру |
| ~~A13~~ | **Снято P34 как разрешительный вопрос.** Остаётся практический факт: в `raw/` ISO 21502 и PMBOK 8 лежат выдержками, PMI AI PPPM отсутствует | Срез 1.6 грузит имеющееся и помечает уровень полноты; полные тексты догружаются тем же путём по мере появления |

### 14.3. Discovery

Выполнен 2026-08-22 read-only: системные ресурсы, версия схемы, определения constraint'ов,
распределение типов источника, объёмы чанков и текста, состояние очереди по двум документам,
сверка снимков периметра. Ни записи, ни DDL, ни перезапусков.

**Не выполнено и требуется до фазы 2:** разбор расхождения 284 / 280 / 277 / 275 (§6);
проверка фактической поддержки отключения retention у обоих провайдеров (A2); замер качества
извлечения на длинном контексте (A9).

---

## 15. Вопросы владельцу

### 15.1. Закрыто 2026-08-22

Все одиннадцать вопросов редакции 4.1 сняты решениями P25-P33 и ранее принятыми P29, P30.
Блокирующих вопросов к владельцу не осталось.

### 15.2. Решения, отложенные по существу

Не блокируют работу; момент принятия определён.

1. **Планка retrieval, доли отказов и claim binding** — ставится владельцем после вертикального
   среза, до масштабирования на 275+ (P29, §13.1).
2. **Источник эмбеддингов** — сторонний эндпоинт (третья модель в смысле P9), локальная модель или
   отказ от векторов. Возникает только если срез покажет нехватку лексического поиска.
3. **Судьба ленты выпусков** — после отчёта расхождений 2.13 (P16).
4. **Наличие полных текстов отдельных стандартов** — практический вопрос к срезу 1.6: грузим
   имеющееся, догружаем по мере появления. Не развилка.

### 15.3. Что осталось до объявления плана исполнимым

Вопросов к владельцу нет, но **контракты ещё не написаны**. Исполнимым план становится после того,
как в фазе 1 будут написаны и утверждены: corpus-membership contract (§6, §6А.4),
ADR-0004…ADR-0008, схема-спека 003 и контракты §9.3. Это работа, а не решение.

---

## 16. Explicit non-goals

- Не планировать вывод Legacy из эксплуатации и смену DNS.
- Не открывать KX наружу: публичного порта, маршрута Caddy и записи DNS у него нет. Доступ к полному
  тексту — только через authenticated editor API с серверной проверкой scope.
- Не публиковать полные тексты чужих материалов.
- Не публиковать авторские тексты и формулировки идей без одобрения владельца (P4). Структурный слой
  под это правило не подпадает (P19).
- Не публиковать элемент, не прошедший fail-closed условия: он уходит в quarantine, а не наружу с
  оговоркой.
- Не выдавать перевод за доказательство: проверка факта идёт по оригинальному span'у.
- Не считать два документа одной семьи или одного дубликат-кластера двумя подтверждениями.
- Не допускать свободный текст модели без привязки к claim в строгом режиме.
- Не называть продуктовое решение владельца юридическим заключением.
- Не расширять egress за пределы двух утверждённых эндпоинтов; всё остальное — deny by default.
- Не расширять список моделей на Local Ru за пределы двух согласованных (P9).
- Не публиковать Hermes наружу и не делать его автором ответа.
- Не использовать общий шлюз Hermes на Local Ru как исполнителя задач базы знаний: только отдельные
  профили.
- Не смешивать публичный `/api/search` Radar V2 с поиском базы знаний.
- Не встраивать KX в путь публикации выпусков V2 — только односторонний read-only.
- Не пытаться исправлять immutable-строки `fetch_attempts`: неверный тип источника компенсируется
  таблицей провенанс-коррекций (D12).
- Не масштабировать на 275+ документов до прохождения вертикального среза (§13.1).
- Не расширять область на 8 038 неотобранных документов без отдельного решения.
- **Не создавать вторую wiki и вторую таксономию**: существующая база AgPM принимается как хребет,
  её схема, конвенции страниц и словарь связей используются без переизобретения (P24).
- **Не переписывать существующие страницы `agpm/wiki/` автоматически.** Машина предлагает
  дополнения и привязки к доказательствам; правка авторского текста — решение владельца.
- Не считать покрытие без отчёта сверки хранилищ (P28).
- Не вводить особых ограничений на хранение и цитирование стандартов и подобных авторитетных
  материалов: они разрешены и согласованы (P34).
- Не открывать архив вопросов читателей шире роли редактора и не оставлять его без механизма
  удаления по запросу (P33).
- Не запускать публичный слой без политики конфиденциальности (P33).
- Не ставить планку качества после масштабирования: она ставится по результатам вертикального
  среза и до него (P29).
- Не вводить git для `knowledge/` в рамках этого плана: версионирование — снимки при публикации
  (P27). Если история между публикациями понадобится, это отдельное решение.
- Не делать редакторский режим основным местом правки wiki: пишет Project Manager (P26).
- Не строить UX первой версии графа вокруг компаний, продуктов, ролей и временной эволюции (P23).
- Не объявлять план готовым к исполнению, пока контракты §6, §8, §9 и §10.4 не согласованы и не
  утверждены владельцем.
