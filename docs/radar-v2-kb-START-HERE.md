# Radar V2 Knowledge Base: точка входа для реализации

Дата: 2026-08-22. Состояние: **фаза 1 выполнена и применена к production; начата фаза 2.**

Этот файл — стартовая страница для новой сессии. Читать первым, затем план.

---

## 1. Что делаем

Публичная база знаний Radar V2 на основе материалов выпусков: wiki, навигация, граф, рейтинги и
диалоговый агент, где каждое фактическое утверждение подкреплено точной цитатой из источника.
Пополняется автоматически после каждого выпуска.

**Канонический план — единственный:**

```
docs/radar-v2-kb-engineering-plan-2026-08-22.md      (редакция 5.4, 1313 строк)
```

Параллельных roadmap нет. `docs/radar-v2-knowledge-base-roadmap-2026-08-22.md` — историческая
запись направления, как план отменён.

**Как читать план:** §1 — 37 решений владельца, все развилки закрыты. §2 — отменённые формулировки,
чтобы они не всплыли из старых цитат. §13 — фазы. §16 — чего не делать. Остальное по ссылкам.

**Важно:** план местами расходится с тем, что реально на диске. Расхождения найдены фазой 1 и
перечислены в §3 ниже. Читая план, сверяйтесь с ними.

---

## 2. Что сделано (фаза 1)

Шесть срезов, три гейта зелёные. Миграция 003 применена к production с подтверждения владельца,
канон загружен, таймеры возвращены.

| Срез | Deliverable | Документ |
|---|---|---|
| **1.1** Corpus-membership contract | `kx/src/radar_kx/corpus_membership.py`, `kx/scripts/corpus_membership_report.py`, `kx/scripts/corpus_membership_kx_extract.sql` | `docs/radar-kb-corpus-membership-contract-2026-08-22.md` |
| **1.2** ADR 0004-0008 | `docs/adr/0004…0008` | там же |
| **1.3** Миграция 003 + импорт артефактов | `kx/sql/003_provenance_and_publication.sql`, `kx/src/radar_kx/artifact_import.py`, `kx/data/provenance-backfill-2026-08-22.json`, `kx/scripts/verify_migrations.sh` | `docs/radar-kb-migration-003-runbook-2026-08-22.md` |
| **1.4** Внутренний поиск и покрытие | `kx/src/radar_kx/search.py`, команды `search` и `coverage-report` | `docs/radar-kb-internal-search-2026-08-22.md` |
| **1.5** Инвентаризация wiki | `kx/src/radar_kx/wiki_inventory.py`, `kx/scripts/wiki_inventory.py` | `docs/radar-kb-wiki-inventory-2026-08-22.md` |
| **1.6** Корпус канона | `kx/src/radar_kx/canon_corpus.py`, команда `import-canon` | `docs/radar-kb-canon-corpus-2026-08-22.md` |

**Гейты.** `kx/scripts/verify.sh` — 133 теста, 35 пропущено (тем нужен сервер).
`kx/scripts/verify_migrations.sh` — те же 35 против временной базы, на обеих базах: репозиторной и
дрейфовой. `v2/scripts/verify.sh` — без изменений, зелёный.

Числа 284 / 280 / 277 / 275 объяснены воспроизводимыми запросами. Гейт §6.3 плана выполнен.

---

## 3. Где план расходится с реальностью

Найдено фазой 1. Всё проверено запросами и тестами.

| План говорит | На самом деле | Где записано |
|---|---|---|
| «7 лишних материалов — это `rejected_materials_internal`» | Неверно: таблица не пересекается с `materials` по URL. 284→277 — это 5 отказов 30-дневного окна плюс 2 снятых коррекцией 2026-06-09 | contract §3.2 |
| «93 страницы концептуальной базы» | Авторских страниц 63 во всей wiki (33 синтез, 25 заметок, 3 обзора, 2 сводки). 32 из 93 — immutable raw | inventory §1.1 |
| «Конвенции страницы — готовая схема `concepts`» | Соблюдены на 5 %: 3 страницы из 63. 257 из 297 заголовков H2 не сводятся ни к одной | inventory §2 |
| Разделы модели `data`, `market`, `maturity`, `open-questions`, `risks` | Пустые каталоги, ноль страниц | inventory §1.2 |
| «Словарь связей переносится в схему графа» | Словарь — да, данных нет: 148 ссылок, все нетипизированные. `concept_links` придётся писать, а не импортировать | ADR-0008 §5.14 |
| «Снимок 40 MB ежедневно даёт 15 GB в год» | Изменяемая часть 4,1 MB при периметре `agpm/**` + `agpm-radar/wiki/**`. Проблемы нет | ADR-0006 §4.18 |
| «Полные тексты есть у AgPMBoK, AWS, двух статей arXiv» | В `raw/` это выдержки, но оригиналы нашлись в `media/inbound/`. После их загрузки цитируемых 29 из 36; заблокированы 7 | canon §3 |
| Три новые ступени добычи | Четыре: добавлен `local_import`, иначе канон пришлось бы записать как операторский артефакт | runbook §1.1 |

---

## 4. Состояние production после 2026-08-22

| | |
|---|---|
| Схема KX | **3** (миграция 003 применена, дрейф D1 закрыт) |
| Релиз | `radar_kx_release_20260822_ea37afef6b28` |
| Документов | 8 349 (8 313 радар + 36 канон) |
| Периметр | 275/275 полных текстов |
| Канон | 36 документов, 827 чанков, `source_kind = 'canon_import'`, 29 цитируемых |
| `verify --full` | ok, errorCount 0 |
| `coverage-report` | ok; дым ru 1 039 ≥ 993, en 662 ≥ 633 |
| Таймеры | оба active |
| Backup | до `manual-20260822T162033Z-pre-003`, после `manual-20260822T162801Z-post-003` |

### Решено владельцем 2026-08-22

Миграцию применить; таймер включить после неё; знаменатель — 275 документов; снимок wiki —
`agpm/**` плюс `agpm-radar/wiki/**`; пушить в main; DOCX читать минимальным ридером;
`why-agpm-v3-executive-rationale` — наш собственный текст.

### Осталось за владельцем

1. **Утвердить ADR-0004…0008.** Читает; статус пока proposed.
2. **2 378 failed в очереди сами не пойдут.** Таймер их не берёт: `claim_tasks` выбирает только
   `pending` и `retry`. Нужен явный `radar_kx requeue-failed`, и это отдельное решение —
   1 745 отказов это `robots_denied` на Reddit, они откажут снова.
3. **Четыре документа из веб-архива** без URL и даты снимка заблокированы для публичных цитат до
   среза 2.3.
4. **Девять документов периметра — это подвал YouTube** (215 символов, `is_complete`). Периметр
   несёт 266 различных текстов, а не 275. Варианты: разбор YouTube как у Telegram, детектор
   boilerplate или снятие с периметра.
5. **Перепечаток между разными изданиями в периметре нет.** Правило независимости источников из
   ADR-0007 в вертикальном срезе проверять не на чем — брать пару из корпуса вне периметра,
   ограничиться дубликат-парой или отложить до 2.4.

---

## 5. Что сделано из фазы 2 и что дальше

Обе работы, которые выбрал владелец, выполнены. Документ —
`docs/radar-kb-vertical-slice-2026-08-22.md`.

**Выборка 2.1 и первый замер retrieval.** 24 документа, 22 хоста, каждый записывает, ради какого
требования взят. Recall@10 = **1,0**, MRR **0,978**, p95 **37 мс** на 23 probe.
Планка не назначена — по P29 её ставит владелец.

**Первая половина 2.5.** 279 утверждений из 34 страниц прогнаны по канону и периметру.
Покрытие терминов: 122 высокое, 133 среднее, 24 низкое. Из 24 девять — открытые вопросы,
доказательство им не нужно; реальная дыра — 15.

### Что дальше

| Работа | Чего ждёт |
|---|---|
| Набор gold-вопросов вида `question` | пишется человеком или моделью с проверкой; сгенерировать нельзя |
| Срез 2.0 — профиль Hermes и оркестратор | **новые systemd-юниты на Local Ru, отдельное подтверждение** |
| Остальные слои среза 2.1 (отказы, claim binding, переводы, инъекции) | упираются в 2.0 |
| Сегментация прозы на 29 страницах wiki | вторая половина 2.5 |
| Независимость источников | нечем проверять: перепечаток между изданиями в периметре нет |

Определения срезов 2.x расписаны в §13.3 плана.

---

## 6. Что нельзя без отдельного подтверждения Ivan

- применение миграции к production и переключение `/opt/radar-kx/current`;
- любой новый systemd-юнит или таймер на Local Ru;
- любые изменения Caddy, DNS, firewall, cron;
- любая запись в production-базы;
- расширение списка моделей на Local Ru за пределы `zai/glm-5.2` и `minimax/MiniMax-M3`.

Полный список запретов — §16 плана.

---

## 7. Факты, которые дорого выяснять заново

**Хосты.** Control-хост — здесь, 4 vCPU / ~5 GB RAM. Local Ru — `root@147.45.99.225`, ключ
`/root/.ssh/local_ru_admin`, 4 vCPU / 7 936 MB RAM / swap 8 GB / 50 GB свободно.

**KX** (замер 2026-08-22 после миграции и загрузки канона): 8 349 документов, 6 828 версий
(6 525 complete), 19 364 чанка, БД 751 MB, очередь 5 933 succeeded / 2 378 failed. Схема **3**.
Периметр текущего релиза `rel_473de6c600563860305e2bd0`: 77 выпусков, 277 строк отбора,
275 документов, полный текст 275/275.

**Как запускать CLI на Local Ru:** `kxrun <команда>` — обёртка в `/usr/local/sbin/kxrun`, задаёт
DSN, `PYTHONPATH` и release id и запускает от пользователя `radar_kx`. Не пытайтесь `source
/etc/radar-kx/worker.env` из bash: значение `RADAR_KX_USER_AGENT` содержит скобки, и bash на нём
падает (systemd читает файл иначе).

**Локальная разработка.** На control-хосте установлен `postgresql-16-pgvector` и создана роль `root`
с SUPERUSER — это нужно `verify_migrations.sh`, который поднимает временные базы `radar_kx_test_*`
и удаляет их за собой. Production это не затрагивает.

**Существующая wiki** — не изобретать заново:
`/root/.openclaw-projectmanager/workspace/knowledge/agpm/` (93 файла, 63 авторских страницы;
`SCHEMA.md` задаёт словарь связей и конвенции) и `.../knowledge/agpm-radar/` (204 файла). Не под git.
Реестр строится командой `kx/scripts/wiki_inventory.py`.

**Модели.** Hermes 0.20.0 на Local Ru, `127.0.0.1:8642`, `zai/glm-5.2` и `minimax/MiniMax-M3` —
облачные API, локального вывода нет. Образец изоляции профилей — юниты NRD на том же хосте.

**Три находки NRD переиспользовать, а не изобретать:** собственный runtime; патч allowlist моделей;
root-owned env 0600 с ключами на профиль. Записаны в ADR-0005 §3.

**Известные дефекты** — §5 плана, D1-D13. Закрыты кодом или зафиксированы: D1 — миграцией 003
(не применена); D9 и D12 — таблицей `version_provenance` и коррекциями; D3 — правилом обращения
в contract §6; остальные открыты.

---

## 8. Перед первым коммитом

Три гейта: `kx/scripts/verify.sh`, `kx/scripts/verify_migrations.sh` (нужен локальный PostgreSQL),
`v2/scripts/verify.sh`.

Если `uv export` выдаёт цветной вывод и `verify.sh` падает на сверке lock-файла — запускать с
`NO_COLOR=1`. Это особенность окружения, а не расхождение lock'а.

---

## 9. Отложенное, с определённым моментом принятия

Планка качества — после вертикального среза и **до** масштабирования. Источник эмбеддингов — только
если лексический поиск провалит замер. Судьба ленты выпусков — после отчёта расхождений. Путь
будущих эксклюзивных материалов — развилка записана в §15.2, решать не сейчас.
