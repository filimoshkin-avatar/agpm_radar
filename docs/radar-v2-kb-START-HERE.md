# Radar V2 Knowledge Base: точка входа для реализации

Дата: 2026-08-22. Состояние: планирование завершено, реализация не начата.

Этот файл — стартовая страница для новой сессии. Читать первым, затем план.

---

## 1. Что делаем

Публичная база знаний Radar V2 на основе материалов выпусков: wiki, навигация, граф, рейтинги и
диалоговый агент, где каждое фактическое утверждение подкреплено точной цитатой из источника.
Пополняется автоматически после каждого выпуска.

**Канонический план — единственный:**

```
docs/radar-v2-kb-engineering-plan-2026-08-22.md      (редакция 5.4, 1313 строк)
```

Параллельных roadmap нет. `docs/radar-v2-knowledge-base-roadmap-2026-08-22.md` — историческая
запись направления, как план отменён.

**Как читать план:** §1 — 37 решений владельца, все развилки закрыты. §2 — отменённые формулировки,
чтобы они не всплыли из старых цитат. §13 — фазы. §16 — чего не делать. Остальное по ссылкам.

---

## 2. Состояние на момент передачи

- Планирование завершено: три независимых ревью, 37 решений владельца, вопросов к нему **нет**.
- Всё закоммичено в `main`, дерево чистое, **не запушено**. Последний коммит `3ea8cdb`.
- **Production не тронут ничем**: ни базы, ни systemd, ни Caddy, ни DNS, ни cron, ни файлов
  сервисов. Единственное обращение к Local Ru за всю работу — read-only проверка базы 2026-08-22
  (только `SELECT` и системная информация).
- Память проекта (`/root/.claude/projects/-mnt-vdd-Radar/memory/`) содержит четыре записи:
  продуктовые решения, дрейф схемы KX, дефекты рубрикатора, рантайм Local Ru.

---

## 3. С чего начинать

Фаза 1, шесть срезов. **Ни один не зависит от ненаписанных контрактов** — начинать можно сразу.

| Порядок | Срез | Почему так |
|---|---|---|
| **1** | **1.1 Corpus-membership contract** | Предусловие всего. Пока не сойдётся знаменатель, ни одна метрика покрытия не имеет смысла |
| параллельно | 1.2 Пять ADR (0004-0008) | Единственное, что требуется от владельца, — утвердить их |
| параллельно | 1.3 Миграция 003 | Пишется и проверяется на восстановленной копии дампа |
| параллельно | 1.4 Внутренний поиск | Работает на существующих индексах, новых зависимостей нет |
| параллельно | 1.5 Инвентаризация wiki | Read-only, входы существуют |
| после 1.3 | 1.6 Корпус канона | Нужны `import-artifact` и `source_publication_policy` из 1.3 |

**Первая задача целиком:** объяснить воспроизводимыми запросами расхождение
**284 / 280 / 277 / 275**, добавив в разбор `agpm-radar/data/materials.jsonl` (8 446 строк) и
`data/source-fulltext/` (75 файлов). Учёт ведётся минимум в четырёх местах. Детали — §6 и §6А.4.

Определения срезов 1.3 и 1.6 расписаны в §13.2 подробно — исполнителю додумывать не нужно.

---

## 4. Что нельзя без отдельного подтверждения Ivan

- применение миграции к production и переключение `/opt/radar-kx/current`;
- любой новый systemd-юнит или таймер на Local Ru;
- любые изменения Caddy, DNS, firewall, cron;
- любая запись в production-базы;
- расширение списка моделей на Local Ru за пределы `zai/glm-5.2` и `minimax/MiniMax-M3`.

Полный список запретов — §16 плана. Там же то, что легко нарушить по инерции: не создавать вторую
wiki, не переписывать существующие страницы автоматически, не масштабировать на 275+ документов до
вертикального среза, не считать покрытие без отчёта сверки хранилищ.

---

## 5. Факты, которые дорого выяснять заново

**Хосты.** Control-хост — здесь, 4 vCPU / ~5 GB RAM. Local Ru — `root@147.45.99.225`, ключ
`/root/.ssh/local_ru_admin`, 4 vCPU / 7 936 MB RAM / swap 8 GB / 50 GB свободно.

**KX** (замер 2026-08-22): 8 313 документов, 6 792 версии (6 489 complete), 19 010 чанков,
59 242 604 символа, БД 688 MB, очередь 5 933 succeeded / 2 378 failed. Периметр текущего релиза
`rel_473de6c600563860305e2bd0`: 77 выпусков, 277 строк отбора, 275 документов, 1 098 чанков,
полный текст 275/275.

**Существующая wiki** — не изобретать заново:
`/root/.openclaw-projectmanager/workspace/knowledge/agpm/` (93 markdown-страницы; `SCHEMA.md`
задаёт словарь связей и конвенции страниц) и `.../knowledge/agpm-radar/` (204 страницы, daily,
monthly, runs, reports). Не под git.

**Модели.** Hermes 0.20.0 на Local Ru, `127.0.0.1:8642`, `zai/glm-5.2` и `minimax/MiniMax-M3` —
облачные API, локального вывода нет. Образец изоляции профилей — юниты NRD на том же хосте.

**Три находки NRD переиспользовать, а не изобретать:** собственный runtime (общий venv резолвится
через `/root` и падает под `ProtectHome`); патч allowlist (Hermes принимает произвольный
идентификатор модели даже при настроенном `model_routes`); root-owned env 0600 с ключами на профиль.

**Известные дефекты** — §5 плана, D1-D13. Самый неприятный: схема production разошлась с
репозиторием после хотфикса `operator_artifact`, и существующие гейты этого не видят.

---

## 6. Перед первым коммитом

Гейт репозитория KX: `kx/scripts/verify.sh` — ruff format, ruff lint, strict mypy, pytest, сверка
locked requirements. Гейт V2: `./v2/scripts/verify.sh`.

Правка `kx/tests/test_schema_contract.py` обязательна при миграции 003: тест ассертит
`'2'::jsonb`.

---

## 7. Отложенное, с определённым моментом принятия

Планка качества — после вертикального среза и **до** масштабирования. Источник эмбеддингов — только
если лексический поиск провалит замер. Судьба ленты выпусков — после отчёта расхождений. Путь
будущих эксклюзивных материалов — развилка записана в §15.2, решать не сейчас.
