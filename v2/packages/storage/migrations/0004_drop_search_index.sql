-- Поисковый индекс никто не читал.
--
-- `published_materials_fts` перестраивался при каждой публикации, его число
-- строк сверялось с представлением при каждом открытии базы — расхождение
-- отвечает 503 на весь сайт, — и ни один запрос API к нему не обращался:
-- с 02.09.2026 поиск идёт по тексту карточки в Python (`_card_search_text`
-- в apps/api/public_data.py), подстрокой, а не по словам индекса. Миграция
-- 0003 переписала источник индекса под поиск, который этим индексом уже не
-- пользовался. Решение владельца 05.09.2026: убрать из контракта.
--
-- Вместе с индексом уходит и представление `pub_search_documents_v1`: оно
-- существовало ради него одного. Если поиск однажды вернётся в SQL, оба
-- собираются заново из issue_materials и material_analysis — ровно так, как
-- это сделала 0003.
--
-- Виртуальная таблица уносит свои теневые таблицы (_data, _idx, _content,
-- _docsize, _config) сама. Логическое состояние базы не меняется: индекс в
-- него не входил (см. contracts/v1/sqlite-contract.yaml, excludes), поэтому
-- указатель релиза остаётся верным.
DROP TABLE published_materials_fts;

DROP VIEW pub_search_documents_v1;
