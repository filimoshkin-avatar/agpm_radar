# Подозрения на повторы и пересмотр состава: выкат 2026-09-19

Код: `68af52f`. KX release: `radar_kx_release_20260919_68af52ff0fcd`.
Существующий `radar-kx-editor` перезапущен, его StateDirectory
`/var/lib/radar-kx/issue-reviews` принадлежит `radar_kx`, права 0700.
Миграции production не применялись. Приложение V2/API не требовало перевыката:
изменения V2 относятся к инструментам контрольного хоста и daily wrapper.

OpenClaw: `agpm_radar_v2_owner_corrections`, ID
`fe004924-20bc-4239-8250-bd1df46925f1`, интервал 120 секунд, command timeout 900
секунд. Использует окружение уведомлений действующего daily-задания; fallback
delivery выключена. Первый исполненный запуск: status `ok`, exitCode 0,
очередь пустая, ошибок нет.

Проверки: полный V2 gate — 404 теста; KX gate и migration gate — exitCode 0;
независимое ревью, восстановление публикации, повтор доставки уведомления,
реальная сборка correction package с исключением карточки и новым анализом.
Браузерный сценарий с тестовыми данными прошёл на ширине 1280 и 390 пикселей.

Production `/po/`: аутентифицированный предпросмотр выпуска 2026-09-19 показал
10 → 9 материалов. Подтверждение публикации не отправлялось. Хеш публичного
выпуска до/после совпал, заявок создано 0, ошибок браузера 0. Без входа API
коррекций вернул 401. Фактическая публикация исключения в production намеренно
не выполнялась без выбора карточки владельцем.

Откат: отключить указанное задание OpenClaw, вернуть прежний KX release и
сервисный файл `/etc/systemd/system/radar-kx-editor.service.before-owner-composition-20260919`,
перезапустить существующий редактор. Не удалять журнал заявок и локальные
пакеты — они нужны для аудита и восстановления незавершённой публикации.
