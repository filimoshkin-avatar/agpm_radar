# Radar V2 documentation

Canonical architecture, migration, acceptance and production runbooks live in the repository-level
[`docs/`](../../docs/) directory. This file prevents the application-local documentation directory
from looking accidentally empty while preserving one source of truth.
