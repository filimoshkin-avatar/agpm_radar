# Разбор отказа анализа за 30 дней, 2026-09-13

Артефакты исходного запуска:
`/root/.openclaw-projectmanager/workspace/state/radar-v2/dual-run-cron/2026-09-13/attempt-001/candidate-build/llm-period-analysis/`.
Отчёт: `2026-09-13/combined-report.json` в том же дереве запусков.
Опубликованный релиз: `rel_4c6db46c404286ff45875a1c`.

Подтверждённая последовательность:

1. Окно 7 дней: 50 материалов, успешный ответ `gpt-5.5` с первой попытки.
2. Окно 30 дней: 211 материалов из 30 выпусков. Все 211 попали в запрос,
   описания сокращены до 160 знаков. Первый запрос — 112789 байт, до лимита
   argv не дошёл. Ответ `gpt-5.5` получен успешно (HTTP 200), но третий тезис
   содержал `egress-контуре`, что корректно отклонил `RUSSIAN_PROSE_GATE`.
3. Вторая попытка `gpt-5.5` также получила HTTP 200. В ней уже написано
   «контролируемом контуре исходящих соединений», но четвёртый тезис содержит
   `agent-first ERP`. Проверка вновь отклонила результат.
4. Третья попытка переключилась на `gpt-5.4`. В сохранённом stderr — HTTP 400
   и сообщение, что эта модель не поддерживается при использовании Codex
   с аккаунтом ChatGPT. CLI вернул код 1. Это установленная причина именно
   этого ответа, а не предположение о временном сетевом сбое.
5. `_fallback()` вставил соединённые ошибки всех попыток непосредственно
   в `rest` каждого резервного тезиса. `period_blocks()` перенёс их в анализ,
   браузер штатно показал эти тексты в разделе периода.

Проверка русского языка усилена коммитом `8561bfd` 12 сентября. Она обнаружила
реальные непереведённые термины. При этом старый механизм восстановления
запрашивал полное переписывание, передавая только последнюю ошибку и не передавая
сам отклонённый ответ. Новая генерация могла устранить один термин и добавить
другой. Резервная модель и вставка ошибок в читательскую прозу существовали
до усиления проверки.

Исправление описано в [ADR-0018](adr-0018-period-fallback-prose.md).
Изменены только генератор периодов, его регрессии и документация.

Операционное наблюдение: `comparisonVerdict.alert=false` в сегодняшнем отчёте
относится к сверке Legacy/V2 и не означает успешный анализ всех периодов.
`v2.periodAnalysis['30d'].status` правильно равен `fallback`.
Штатный `run_stage15_daily.sh` уже включает статусы периодов и причины отказа
в текст уведомления, если его отправка настроена. По этому отчёту нельзя
подтвердить доставку уведомления. Для мониторинга качества нужно читать
статус каждого периода отдельно от успешной публикации и сверки состава.

Ограничения: повторные запросы повышают шанс исправления, но не гарантируют
доступность провайдера или безошибочную прозу. Непроверенную запасную модель
нельзя считать работоспособным резервом. Сегодняшний опубликованный выпуск
в рамках этого разбора не перепубликовывался. Для его восстановления достаточно
пересчитать месячный блок и выпустить коррекцию; повторный сбор не требуется.

Проверка сохранённых первых двух ответов воспроизводит исходные отказы.
Замена только `egress-контуре` / `agent-first ERP` русскими формулировками
позволяет каждому ответу пройти текущий валидатор. Это подтверждение причины
отказа, а не независимая проверка фактов и не опубликованная редактура.
Результат локальной проверки: `/tmp/radar-period-retained-replay.json`.

Проверки рабочего кода:

- Целевые тесты периодов и русского языка: 25 passed.
- Полный V2 verify: exit 0, 322 passed; Ruff, mypy, контракты, фронтенд,
  изоляция и артефакт сборки также прошли.
- KX verify: exit 0, 603 passed, 204 ожидаемых skips.
- KX verify_migrations: exit 0, весь набор из 807 тестов прошёл на временных
  локальных БД. Миграции production не выполнялись.
- Логи: `/tmp/radar-period-{v2,kx,migrations}-verify.log`.

## Коррекция по уточнению владельца

Владелец поручил заменить сегодняшние технические тексты детерминированным
уведомлением и сохранить причины отказа в сообщениях OpenClaw. Вместо четырёх
резервных псевдотезисов подготовлено одно сообщение; подпись интерфейса больше
не показывает «LLM недоступна» или счётчик тезисов для отказа.

Проверенный пакет: `/tmp/radar-period-correction-20260913-v2/`, кандидат
`cand_correct_20260913_reader_notice_v2`. Первый пакет не прошёл проверку
связи анализа с материалами и не публиковался: публичная проекция выдаёт
идентификаторы рубрик в другом порядке, чем исходный кандидат. Во втором
пакете внутренний `inputContentHash` пересчитан для того же состава в порядке
проекции. Сами рубрики и остальные поля материалов не меняются.

Проверка точного публичного результата разрешает только замену четырёх
месячных текстовых блоков одним уведомлением. Дневной анализ, недельный анализ,
все материалы и исходные метаданные отказа совпадают с опубликованными.
Публичные документы остальных 98 выпусков также совпадают целиком. Таблицы
материалов проверены побайтно за исключением служебных временных отметок.

Регрессия `_period_statuses()` подтверждает сохранение исходной ошибки для
суточного уведомления OpenClaw; отправляющий скрипт не изменён. Браузерная
проверка подготовленного выпуска прошла на 1440 и 390 пикселях, включая
переходы день → месяц → неделя → день, без ошибок JavaScript.
Финальный V2 verify: 323 passed; KX verify: 603 passed / 204 expected skips.
Логи этого этапа: `/tmp/radar-reader-notice-{v2,kx,migrations}-verify.log`.
